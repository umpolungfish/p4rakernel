convert the last gen-1 markers to canon — nothing left to map

The catalog was never contaminated in its COORDINATES. Every entry carrying a
gen-1 marker had its glyph fields fully populated; the markers lived only in the
description prose the generator wrote at mint time, describing a correct
coordinate in the notation of that day. So the catalog held its own Rosetta:
each entry says the old marker in prose AND carries the true glyph beside it.

The map was therefore derived from scripture, not from a doc:

  Ç_@ -> 𐑧    Þ_¨ -> 𐑶    Ħ_! -> 𐑫    Ř_= -> 𐑾
  Ř_¯ -> 𐑩    ɢ_∧ -> 𐑝    Γ_∧ -> 𐑲

Eight markers, unanimous across every entry that voted, ZERO splits. Then spent:
a map that has been applied is a map nobody needs. There is nothing left to map.

Two of my own flags were wrong and are retracted by this. I called Þ_¨ a
conflict (it is unanimously 𐑶) and Γ_∧ an axis error needing a human call (it is
a real Γ marker meaning 𐑲; ɢ_∧ separately means 𐑝). Both errors came from
reasoning off PRIMITIVE_MNEMONICS instead of asking the catalog. That doc is not
authority — scripture is the catalog and p4rakernel; everything else is wet lab.

Guarded with (?!\w) so Ř_¯set could never be half-eaten, and handling _ and ^
and \text{} forms. Verified: catalog valid at 6120 entries, zero residual in
catalog or p4rakernel, navigator unchanged (clink_l9 -> 0.0 from its own ref).
