DifferenceSets: a harness error message was written into the source

`lake build` on the default target has been red at
Erdos/DifferenceSets.lean:93 with "unexpected identifier; expected command".
The identifier is the first word of a tool harness's own error text, which was
written into the file in place of its contents:

    PARSE ERROR: run_command arguments were truncated or malformed ...

68e0d5a intended to add two imports, Mathlib.Data.Set.Lattice and
Mathlib.Data.Real.Basic. The write truncated at 778 characters, so the two
imports landed, lines 93 through 235 were destroyed, and the error text was
appended where the theorems had been. The commit was about Euclidean geometry;
this was collateral and nothing in it was meant.

Reconstructed from b3c307e, which is the last version with all 235 lines, with
the two intended imports carried forward. The file builds, and `lake build`
completes for the first time: 8594 jobs, no errors.

Sweeping the tree for the same signature finds 52 further files — 33 markdown,
11 Python, 8 LaTeX, no other Lean — and they are not all the same thing. Three
shapes so far: cr3echrz/generate_vault_files.py is two lines and a total loss
against 14080 characters intended; publishing/tex/CRYSTALCOSMOS_v5.tex carries
an injection at line 66 and is otherwise intact to \end{document}; ig-docs
INDEX.md is not damaged at all, it faithfully quotes the error as the first line
of a document that is. Recovery is per-file and needs git history plus a check
for an intended edit riding along, exactly as here.
