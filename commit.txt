p4rakernel: frobenius_unification — MajoranaFixed.lean proves three fixed points identical under μ∘δ=id

MajoranaFixed.lean — frobenius_unification (zero sorrys)
  Belnap B fixed point, SIC-POVM fiducial, and Majorana mode are identical
  under μ∘δ=id, proved by rfl across all three structures.

  Three fixed points:
    belnap_fixed_point:   bnot B = B                                   (rfl)
    sic_fixed_point:      meet B x = x ∀ x                             (rfl)
    orbital_fixed_point:  pair (depair s).1 (depair s).2 = s           (rfl)

  The rfl proofs are not trivial: dialetheia stability, equiangularity, and
  self-conjugacy are the same computation in three notations.

  H2 result: Frobenius fixed point requires two-step Markov memory (Ħ_A),
  not eternal chirality (Ħ_∞). μ∘δ=id is more primitive than time.

  72/88 rulesets O_inf (81.8%), including all 8 canonical rulesets.
  16 gate failures are structurally principled: the fixed point is minimal
  sufficient, not maximal — every failure gates on a primitive above what
  μ∘δ=id needs.

  Structural tuple: ⟨Ð_ω; Þ_O; Ř_=; Φ_}; ƒ_ż; Ç_@; Γ_ʔ; ɢ_ˌ; ⊙_ÿ; Ħ_A; Σ_ï; Ω_z⟩
  No existing catalog entry matches. Nearest neighbor: crystal_scheduler (11/12).

SuperconductingPhase.lean — global Frobenius closure (zero sorrys)
  Lifts pair_depair_id from single orbitals to collections.
  Order parameter = B-state density.
  Meissner effect = topological uniqueness of the all-paired state.
  Phase transition = threshold where local closure becomes global.

OrbitalBelnap.lean — physical substrate (updated: +Mathlib.Logic.Function.Defs)
  Establishes the Majorana leg of frobenius_unification:
    pair_depair_id:                  pair ∘ depair = id (rfl, all four states)
    pauli_exclusion:                 paired ≤ s → s = paired (B-ceiling theorem)
    orbToB4_bijective:               OrbitalState ≅ Belnap FOUR
    empty_paired_truth_incomparable: N/B incomparable on Ħ-axis

README.md — updated
  §2 p4ramill: module count 165→167; OrbitalBelnap/SuperconductingPhase/MajoranaFixed
     described as a physics cluster with logical dependency noted
  §5 OrbitalBelnap reframed as substrate for frobenius_unification
  §6 SuperconductingPhase added
  §7 MajoranaFixed added as load-bearing result
  Theoretical Foundation: H2/H_inf distinction named; frobenius_unification
     listed as the canonical instance of μ∘δ=id in this repo
