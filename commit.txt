Erdos-Straus: descent in the form the frontier reading uses

straus_of_representable_divisor: a representable proper divisor represents n.
That is straus_scaling with the denominators existentially quantified, and it is
the Lean statement of why a composite survives the frontier only when every
factor does — which is what the instrument's on_frontier computes and why 622 of
624 frontier values below 200000 are prime.
