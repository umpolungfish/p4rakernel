feat: SIC_D12_MagnitudeClasses: the magnitude field is K16(sqrtN0,N1,N3,N5,N9), exact in Lean

First tower plank of the d=12 existence construction (route sanctioned by the
d12-existence ob3ect batch, 7/7 Frobenius PASS; workspace cont.15). The twelve
fiducial magnitude roots all lie in the five-cover tower
K16(sqrt N0, sqrt N1, sqrt N3, sqrt N5, sqrt N9), degree 512 over Q, because the
dependent moduli pair into SINGLETON square classes:
[N2]=[N4]=[N6]=[N8]=[N10]=[N0], [N7]=[N5], [N11]=[N1].

The module machine-checks the seven exact witnesses C_k in K16 with
C_k^2 = N_k * N_i (native_decide; K16 multiplication = flat polynomial product
mod pr, reusing the SIC_D12_Equiangularity Poly engine and the SIC_D12_Norm
moduli vectors - no data duplication). Hence sqrt(N_k) = C_k*sqrt(N_i)/N_i.
Witnesses recovered by nfroots in PARI (exact field computation, not a floor),
re-verified in Python fractions by the generator (gen_lean_magnitude_classes.py
in ~/imsgct/d12_sic_build; the Lean data block is generated, never hand-entered).
Coefficient numerators under 18 bits. Registered in lakefile.toml; bare
`lake build` green (8340 jobs).
