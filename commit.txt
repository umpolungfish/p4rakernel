Anti-Ramsey odd cycles: a piecewise formula is not a dialetheia

The verdict read "B — Both True and False depending on k", calling the C₃ and C₅
cases a dialetheic breakdown. A quantity taking different values at different
arguments is a function. regime_of_k is one, and regime_partition proves the
three regimes partition k with no k claimed by two of them, so no proposition
here holds both True and False. The kernel's ffuse on (T,T) is T and returns B
only from a genuine T/F opposition, which a partition cannot produce. The
transition at k=3 is a change of order, not a bifurcation of one claim, and
regimes_separate shows the linear formula falls below the quadratic one from
n=8 onward.

The threshold the problem sits one edge above is now named rather than quoted.
Mantel's extremal graph is the balanced complete bipartite graph, and its edge
count ⌈n/2⌉·⌊n/2⌋ equals ⌊n²/4⌋ — turan_edges, proved by splitting on parity,
with the odd branch needing the left side normalized before the product is
abstracted. So "one edge above the Turán threshold" is a statement about a
specific graph plus a specific edge.

Standard axioms throughout. Library green at 8585 jobs.
