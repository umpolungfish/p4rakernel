Novikov and Baum-Connes against the machinery the Grammar already carries

I wrote that these two want characteristic classes and an assembly map that no
local definition supplies. That implied a lack the Grammar does not have.

FrobeniusAlg in IGFunctor.lean IS an assembly: comul localises, mul assembles,
and frob says the assembly reconstitutes what the splitting produced — which is
what an assembly map being an isomorphism asserts. The characteristic class is
the winding: an integer invariant of a closed object, carried on the twelfth axis
and read by the winding lattice.

So AssemblyReconstitutes and AssemblyIsIso are stated over an arbitrary Frobenius
algebra, and ig_assembly_reconstitutes PROVES the Grammar's own case on propext
alone — igFrobeniusAlg.frob, discharged by mu_delta_A_id. What stays open
elsewhere is the same statement where the splitting is not diagonal.

Novikov is WindingInvariantUnderClosure: an integer invariant unchanged by every
transformation preserving the closure. That is the homotopy invariance of a
higher signature, said in the register that has the objects.

Build green at 8593 jobs.
