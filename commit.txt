Erdos-Straus: two more blocks of the frontier

200001-400000 has 486 frontier values, 484 of them prime, 1.5% of the surviving
class; 400001-600000 has 444, 440 prime, 1.3%. Both blocks are closed entirely by
a cofactor of M at a rung no larger than 200, so 2521 remains the only value
anywhere read that genuinely needs a divisor of M^2. Largest least-k seen is
15083, at n = 297889 on rung 79.

The frontier's share of the class is falling as the range moves out, and the
cofactor form keeps pace with it.
