The S matrix is symmetric and squares to the identity.

Both labels are self dual so charge conjugation is trivial and unitarity collapses to the square being the identity, proved case by case over a closed form for the two-object sum. The suggestion that the wildcard in the tau-tau line is unsound does not apply: the type has exactly two constructors, the wildcard covers both, and both multiplicities are one.
