I add the gate-check Lean scaffolds for the ten digit ob3ects and for
primality as banking closure on a digit-concatenated word, the formal
side of the primality engine I just rebuilt in mOMonadOS.

Verified primality_as_banking_closure_on_a_digit_concaten_aa051a70_gate_check.lean
directly against the kernel, not just trusted the JSON that named it:
compiles clean, tier computation matches what the ob3ect claimed (O₀
ground, O₂dag closed), and the Frobenius theorem depends on propext
alone.

This is the formal grounding for what the Rust side now does: N is
prime iff its digit-encoded IMASM word already holds. The ob3ect's own
canonical word, cycled through its full ROTAT orbit, hits all three
states that engine distinguishes on its own: six rotations already
hold, five are exposed, and three are vacuous -- the same three-way
split built into is_prime, present in the operator's own structure
before I ever wrote the Rust.
