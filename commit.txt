Prove time is a derived object: T = Work(T) as least fixed point

The capstone. Time is not a thirteenth primitive; it is the least fixed point
of the Work operator, the trace of the operad, which can only close once the
gates have fired. TimeFixedPoint.lean builds it on GateOrdering.

Work is the trace traceC, so T = Work(T) is the loop closing on itself.
work_fixed_iff_special: the fixed points are EXACTLY the Frobenius-special
class pol=or and crit=monad — nothing off it is time. time_least_fixed_point:
lfp_trace (the special tuple with all ten free axes at bottom) is a fixed
point and lies below every fixed point in the componentwise product order
imsLE assembled from the twelve axis LinearOrders — concrete Knaster-Tarski on
the finite lattice. no_time_before_frobenius is the sharp claim: if parity is
not yet at or, the equation has NO solution, so time cannot be posited before
stage 4 — it is derived, not primitive. time_has_passed_gates, sealed_is_time,
and stone_is_sealed_time close the loop to G3 and the Stone. time_is_derived
bundles the lot.

Builds green, zero sorries, no native_decide; all kernel-checked. Registered
in lakefile.toml.
