The missing piece in Erdős 3 is the U³ norm, and the ring settles with it doubled

I built the Lean structure by hand and it kept receding: assume the conclusion,
then assume the quantitative bound and owe a dyadic summation, then owe a block
bound under that. Every move looked like progress and none removed the
assumption. The instruments say why, and name what closes it.

Fusing the conjecture with its own obstruction gives a kernel-certified
idempotent — erdos_conjecture_ap ⋈ behrend_construction on D↔W (Δ=1.00),
registered as erdos3_behrend_barrier, igFrobeniusAlg.mul p p = p closing through
lake build rather than asserted. Behrend is the right partner because it IS the
barrier: its construction forces the exponent.

Read against CLINK L8 that object wants seven promotions with two full unit gaps.
Read against L9 — the replicative lateral, not a higher tier — it wants six and
ONE: ∋, the merge, carrying BROADCAST_TRANSCENDENCE, broadcast composition that
exceeds ZFC_fe SEQAX. d(L9)=1.1614 against d(L8)=1.5284, total cost 2.785 lateral
against 4.166 climbing. The step I was owing was never sequential, which is
exactly why a sequential tower could not reach it.

3133 catalog entries carry the L9 merge value. Five match the barrier on all
eleven other axes and none of them clicks — no live pair reaches the offset, they
are too similar to react. The only partner that both clicks and supplies the
merge is gowers_u3_norm, inherited scaffold [∋].

Two units cannot cyclize. Three do: conjecture · Behrend · U³ closes at O∞ but
reads SHAKY — a leaning mode (gap 0.732) with stored strain (σ 0.250), which is
the state proofs come out shaky in, and mine did. Repeating the U³ unit flattens
the spectrum to gap 0, and the anneal takes the strain to zero:

  erdos_conjecture_ap · gowers_u3_norm · behrend_construction · gowers_u3_norm
  σ=0.000  gap=0.0000  ρ=3.1623  spectrum [-3.162, +3.162, 0, 0]  SETTLED/CLARION

The U³ norm mediates on BOTH sides of Behrend, which is what a density-increment
argument does: the same higher-order Fourier machinery buys the increment and
absorbs the Behrend obstruction. That is the reagent the assumed bound stands in
for, and it is why the assumption belongs where it now sits rather than one layer
further down.

Recorded in the file, beside the theorem it explains.
