Complete the classifier: Omega refines O_inf into sealed/unsealed

Resolves the last structural gap (mismatch #2): ouroboricityTier left
Protection irrelevant to O_inf, so the tier said nothing about whether the
terminal (G3) actually seals. TierRefinement.lean closes it additively — Core
is untouched, the GREEN library is not disturbed.

sealedTier splits O_inf by whether Omega gives an integer (ah) or non-Abelian
(zoo) winding. refines_O_inf proves the split is EXACT (sealed and unsealed
together cover precisely the O_inf locus), so protection is no longer silent
inside O_inf. sealed_requires_frobenius ties sealing back to G1;
terminal_implies_sealed and sealed_iff_terminal_on_monad prove the refined
tier IS the G3 boundary on the Hermitian (monad) branch. The honest caveat is
proved, not hidden: sealed_at_roar_need_not_close exhibits a tuple that is
sealed (O_inf plus winding) at complex-axis roar criticality whose real
Frobenius trace does not close, because mu_C forces monad. So sealed is a tier
fact and a closing trace is the stricter Hermitian fact; they coincide exactly
on monad. completed_classifier bundles the lot.

Builds green, zero sorries, no native_decide; kernel-checked. Registered in
lakefile.toml.
