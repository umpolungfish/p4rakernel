UNSOLVED: discrete geometry and Diophantine equations

Cap sets over F_3^n with the collinearity written as a zero sum, convex position
through the convex hull, twice the triangle area as a determinant so Heilbronn
gets its two-sided bound, and Hadwiger's covering by homothets. Each quantity the
source named — maxCapSetSize, IsConvexPosition, maxMinTriangleArea — is built
here rather than assumed.

The source's cap set bound wrote (3/C)^n with C existentially quantified after
the inequality, which any C near zero satisfies. It says c^n with c < 3 now.

Its Erdos-Straus entry reads 4/n = 1/x + 1/y + 1/z over natural division, where
the left side is 0 for n > 4 and the right side is 0 for x,y,z > 1 — the
statement holds for reasons unrelated to the conjecture. Not restated here:
Erdos.StrausGreedy.IsThreeUnit carries it over the rationals with the ladder
around it, and a second copy is drift.

Factoring in polynomial time now says polynomial in the BITS of n. Polynomial in
n itself is trial division.
