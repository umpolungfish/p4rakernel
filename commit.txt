The displacement exponent, as the pair the object fused to

The seventh ob3ect fuses this one to B: the exponent is constructively estimated
on the finite regime and constructively unattainable in the limit, and its FFUSE
keeps both arms rather than choosing. So the file carries both — a bracket that
finite computation establishes on the mean squared displacement, and a limit
statement no finite computation reaches — with DisplacementKnownState holding
them together.

The walk is written out too: a list of lattice points, no repeats, consecutive
points a unit apart, taken by zipping the list with its tail.

My first draft of the bracket ended in True, which is the exact shape this pass
exists to remove. It states the two inequalities now.
