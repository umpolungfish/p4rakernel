Align the verdict map to Core's tier rule table; archive the pre-migration Primitives tree

VesselContent's criticality_verdict and isOInf admitted only `monad`, while
Core's R1 admits the R2 escape set {monad, roar}. A roar+or' vessel was therefore
O_inf by the rule table and N by the verdict map: the same vessel at terminal
self-referential closure and unmarked at once. Core is canonical, so the verdict
map is realigned rather than the rule table.

- criticality_verdict and isOInf now key on {monad, roar} x or', matching R1.
- isOInf_iff_tier_O_inf proves the vessel predicate and the rule table name the
  same set, in both directions. The reverse direction discharges against Core's
  o_inf_requires_phi_c and o_inf_requires_P_pm_sym rather than re-deriving the
  case analysis, so the two cannot drift apart again silently.
- criticality_verdict_classify gives the total classification over
  Criticality x Polarity; oinf_iff_verdict_T restates the T-locus over the pair.
- The residual conjunct of WithinAlgebra now carries content: consciousnessScore
  is proven three-valued, and residual_pos_forces_phi / residual_gt_half_forces_k
  show positive residual excludes the subcritical region and residual above one
  half pins both gates open. frozen_brain_rejects_residual is the concrete
  unreachability witness.
- oinf_verdict_T's proof is replaced. The old one split on c.crit while the
  hypothesis still spoke of c.crit, so no branch became contradictory and every
  `contradiction` had nothing to close.

The dagger rule was numbered R2 in ouroboricityTier's docstring and inline
comment, colliding with the R2 already in the header table. It is now R6 and
appears in the header table, here and in CLINK_L9.

The package-root Primitives/ tree is a duplicate of Imscribing/Primitives/ that
left the build before the alphabet migration and kept the retired spellings
(⊙^ℂ, Ω_0) in its copy of the tier rule table. Nothing imports it and the
lakefile does not glob it. Moved to archive/Primitives-pre-migration/ with a note
recording why, rather than deleted.

README gains a note that phi_c_gate tests ⊙, not Φ. The prefix is vestigial; the
rename is deferred because it touches every module that unfolds
consciousnessScore.

Pre-existing and untouched: Millennium/ProofModules/* fail on imports of Mathlib
modules that do not exist on disk.
