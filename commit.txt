SelfNest: lift the proof of the law and hand it to the law

mu_delta_A_id and mu_delta_B_id lift to 86,065-glyph words and verdict T through
vox. The prediction came from FIXED_POINT_NESTING_RULE before the run: the proof
of mu-after-delta = id states what the auditor computes, so it is a fixed point of
the auditor, so the nest closes in one shot. It did.
