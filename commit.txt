A reader can now watch the moduli result check itself.

The three modules built, but nothing in the repository turned that into something a person could run. verify_sic_moduli.sh elaborates them and prints the claim list, then the axiom dependency of each headline theorem, so the line between proof and PARI-computed field data shows up in the output instead of having to be reconstructed from the sources.
