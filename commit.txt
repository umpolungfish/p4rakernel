Fill the four gate-checks with real content instead of leaving them as scaffolding

KastrupConformalGroup, GlattfelderSelfContainmentPrinciple,
IUTClosedWalkAtOInfinity, and ScholzeStixBooleanProjectionAtO2 have sat
since they were auto-designed carrying nothing but the generated
IGProtocol scaffold and one Frobenius self-fusion theorem. That's not
material, it's the skeleton the material was supposed to go into.

Checked something first that I'd noticed but never verified: all four
files' closing Imscriptions are byte-for-byte identical. Confirmed
directly against each file's own definition, not by eye. Four completely
different concepts, a symmetry group, an information-theoretic principle,
a number-theoretic proof technique, and a logical objection to it, auto-
design to the literal same term.

Added to each of the four files:
- A theorem naming the precise reason the word doesn't reach O_inf: it
  carries the critical mark the tier needs (crit = monad) but not the
  polarity alongside it (pol = or': here pol = church), the exact half of
  o_inf_iff_P_pm_sym_at_phi_c's conjunction that fails.
- The word's real distance from the canonical self-containing fiducial,
  odotOperator: primitiveMismatches = 6, checked, not asserted.

GlattfelderSelfContainmentPrinciple additionally gets the actual self-
containment argument the file is named for: igFrobAlg_self_fusion is
universal (every Imscription self-fuses, since tensorProduct compares a
value to itself and always ties), so that alone proves nothing
distinctive about this word. What's distinctive is that it sits strictly
beneath odotOperator in the tier order (igFrobAlg_unit_at_top), the
actual shape of "self-containment is the ceiling, this word is not the
ceiling."

ScholzeStixBooleanProjectionAtO2 additionally gets the precise
correction its own name needs: the word lands at O₂dag, not the bare O₂
the name reaches for, and a direct counterfactual proof that changing
only the dim axis away from array flips it to plain O₂ -- confirming
dim is the one axis responsible, not an accident of everything else.

New file: FourClaimsSameClosure.lean, importing all four gate-checks and
proving the shared-closure claim against their real definitions (rfl on
each pair) rather than four independently copy-pasted literals trusted to
agree, plus that the shared term forces a shared tier and names it as
O₂dag directly.

Making the cross-file proof possible required widening exactly one
binding per file, the closing state itself, from private def to def --
nothing else in any of the four files changed visibility. lake build
doesn't track these GateCheck files as library targets, so getting Lean
to see the four states as real imports meant producing their oleans
directly (lake env lean -o) rather than through lake build; that's a
local build-cache detail, not a repo change, .lake stays gitignored.

Verified all five files individually with lake env lean against the
built mathlib cache: every new theorem checks, all four still report
the same O₀ ground / O₂dag closed tiers as before this change, zero
sorry anywhere. Full lake build (8639 jobs) also passed clean with these
edits in place.
