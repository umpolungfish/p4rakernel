Erdos batches 2 and 3: the same repair

Batch 2 loses twenty-one statements and batch 3 sixteen, all of them entries that
asserted something true of everything: "there exists N with N <= 4k+1", "n/n
tends to 1", "aleph one is at most its square", "there is a transversal" with no
bound on its size, and the negation of a statement ending in True — that last one
being FALSE while recorded as B.

What survives is restated with its object. Hindman's function against a tower of
twos with the monochromatic finite-sums set spelled out; the LCM triple count as
a count of triples; Cameron-Erdos as a two-sided bound on the number of sum-free
subsets; average degree forcing a vertex of degree three rather than of positive
degree.

One became a theorem: bounded degree bounds the edge count linearly, which the
handshake identity gives outright, so edges_le_of_degree_le proves what the Prop
was citing.

Verdict tables and their counts are recomputed from what each file defines. A
count of a table is only worth stating if it moves when the table does. Library
green at 8592 jobs.
