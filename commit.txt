fix: restate IsSICPOVM to the satisfiable unit convention; give lake a default target

Two integrity defects found while pinning the existence-discharge target.

1. IsSICPOVM (SIC_POVM_Stark.lean) paired `wh_normSq d fiducial = d` with
`‖wh_inner ...‖ = 1`. That pair is UNSATISFIABLE: the WH displacements form an
orthogonal operator basis, so sum_{a,b} |<psi|D_{a,b} psi>|^2 = d*‖psi‖^4
(= 1728 at d=12, ‖psi‖^2=12) while the fields force d^2 + (d^2-1) = 287. So
crystal_forces_d12_sic asserted a falsehood, and the shadow axiom pair
(equiangular_from_stark_axiom + norm_of_normalized_axiom), stated about the
same concrete vector, was jointly inconsistent with a formalizable Mathlib
fact - a soundness landmine, same Bucket-A family as the OPN/BSD/NS
restatements from the 2026-07-01 sorry audit. Restated to the standard unit
convention: norm_eq ‖psi‖^2 = 1, equiangular (d+1)*‖O‖^2 = 1 (rational form).
This matches the 1500-digit fiducial data, the manuscript, and what
SIC_D12_Norm / SIC_D12_Equiangularity machine-check. Both shadow axioms and
their passthrough theorems updated; sic_povm_exists_via_stark unchanged.
All wh_inner/wh_normSq consumers are inside SIC_POVM_Stark.lean, so the edit
surface is one file plus the crystal_forces_d12_sic docstring note.

2. lakefile.toml had NO defaultTargets, so a bare `lake build` built NOTHING
and always reported "Build completed successfully (0 jobs)" - the deepest
stale-green trap yet: prior sessions' bare-build "green" observations were
no-ops (module-name builds were real). Added defaultTargets = ["Imscribing"].
Bare `lake build` now compiles the lib: Build completed successfully
(8339 jobs), warnings only, whole lib green with the restated convention.
