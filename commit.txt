feat: SIC_D12_ExistenceRing — all 143 overlap identities derived exactly in the constructed ring, in Lean

The existence-grade Lean plank for the d=12 axiom drop: the whole fiducial lives in
R = K16(s0,s1,s3,s9, i, c5, u1), dim 2048 over Q, and native_decide checks, from the
constructed tower (not pinned data): the relation web (rho^2 = N1*N5*D5 cover collapse,
c2/s2 in K16 making u1 quadratic over K16(i), zeta12 in K16(c5,i), unit phases),
zbar_k z_k = N_k for all 12 coordinates, trace-one norm, and ALL 143 WH overlap
identities O_{a,b}*conj(O_{a,b}) = 1/13 (stratum_0..11 + existence_identities_all).
Generated by gen_lean_existence.py (d12_sic_build cont.20); the generator runs the
full Python fraction engine as its re-verify gate before emitting.

Every Q-algebra component of R is a harmless branch combination (flip audit), so these
ring identities mean any ring hom R -> C sends the formal coordinates to a genuine d=12
SIC fiducial. Remaining for the crystal_forces_d12_sic drop: the embedding capstone
(exhibit one hom R -> C: real root of the K16 polynomial, positive square roots, real
c5 root, u1 = sqrt(E2)).

Registered in lakefile.toml; module builds green; full lib green (8341 jobs).
