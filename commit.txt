SIC_D12_Embedding: equiangular_bridge healed and green; crystal_forces_d12_sic DROPPED from axiom to theorem.

I fixed the four transcription defects in the never-compiled bridge block: two explicit rfl closures where rw's auto-rfl could not unfold psi (psi k is phi (rZ k.val) definitionally); the parenthesis-broken sum-conversion lambda replaced with a unifying placeholder in Fin.sum_univ_eq_sum_range; and hjk inserted into the rewrite chain so the omega-exponent follows the psi-index into (k+12-a) form (hpsi alone left the exponent behind and hphase would never have matched).

With the bridge lemma proven, the capstone closes: d12_sic_exists : IsSICPOVM 12 psi and crystal_forces_d12_sic : SICPOVM_Exists 12 are theorems. Axiom audit is clean: propext, Classical.choice, Quot.sound plus Lean.ofReduceBool/Lean.trustCompiler (native_decide), no project axioms, no Stark shadow.

SIC_POVM_Functor now imports the Embedding capstone and restates crystal_forces_d12_sic as a theorem delegating to it. The axiom is gone. Module and full library both build green (lake exit 0, fresh oleans). The d=12 SIC existence is machine-checked end to end: exact fiducial in R = K16(s0,s1,s3,s9,i,c5,u1), transferred to C along phi at the IVT-bracketed root g0, norm and all 143 overlap identities riding the frozen native_decide planks.
