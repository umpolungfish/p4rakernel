Landau's g(n) computed, with the partition search proved complete

The order of a permutation is the lcm of its cycle lengths, so g(n) is the
largest lcm over partitions of n. That is the kernel's One-Shot #1 in the
symmetric group — the period IS the winding number — and it is what Ω=ah was
already recording on this tuple.

Partitions are generated with a fuel parameter, so termination is immediate, and
the generator is proved complete: every non-increasing partition of n with parts
at most k is produced. partsF_complete is the induction, and partsF_sum is the
soundness half. Together they turn the computed table into a theorem about all
cycle types rather than about the ones the search happened to visit — the same
discipline the g₃ search was put under.

g(1..10) = 1,2,3,4,6,6,12,15,20,30. g(7)=12 comes from 4+3 and not from the
7-cycle, which is the first n where the maximum order is not a single cycle;
g(8)=15 by 5+3 and g(9)=20 by 5+4.

The heavy tail the file describes as "~√(n log n) in the exponent" is now
stated as the inequality it is. Landau's exponent √(n log n) dominates
Erdős-Turán's (1/2)log²n exactly when log³n ≤ 4n, and exponent_gap proves that
equivalence with L standing for log n.

Standard axioms except landau_table, which is native_decide. Library green at
8585 jobs.
