Collatz: the crystal closes one slot from CLINK L8, and that slot needs the one operation
the toolchain does not expose

Adding the non-abelian carrier to our macrocycle gives a four-unit set that CLOSES:

  --crystallize collatz_deep self_referential_imscription this_imscription_is_false maximal_nn
    lattice: [self_referential_imscription · collatz_deep · this_imscription_is_false · maximal_nn]
             closed crystal, O∞
    mother-pool: none — the whole pool crystallized
    ✓ T: a clean crystal — every unit fits and the lattice closes.

  --fpt on the same set: nothing outgasses, the assembly is fully bound. ✓ T

Its blend, max per axis as click.rs defines the operation:

  blend     ⟨𐑦𐑸𐑾𐑹𐑐𐑧𐑲𐑵⊙𐑫𐑳𐑟⟩
  CLINK L8  ⟨𐑦𐑸𐑾𐑹𐑐𐑧𐑔𐑵⊙𐑫𐑳𐑟⟩

ELEVEN of twelve slots identical, d(L8) = 0.4038. The single gap is ∈ 𐑲 against 𐑔 --
ℵ against ℷ, aleph against gimel -- and it needs LOWERING. It is the same obstruction
that made L9 unwritable by the derivation, arriving now from the other side.

Every lowering operation, tried and exhausted:
  --anneal      both the 3-ring and the 4-ring are ALREADY at σ(Δ)=0.000, exhaustive
                over every ordering. No relaxation available. (--cleave calls the same
                4-ring FRUSTRATED at ρ=2.0 -- two metrics disagreeing on one ring.)
  --fpt         nothing outgasses
  --cleave      fission opens both daughters
  --crystallize nothing rejected
  --descend     lowers ⊙ and removes a winding, not ∈
  meet          not exposed between two entities anywhere in the toolchain

So the remaining distance from a closed, crystallized, kernel-certifiable Collatz object
to CLINK L8 is ONE slot requiring ONE meet, and `ask` has no --meet. That is the same gap
recorded in cfb7ef1, now at its sharpest: not a research programme, a missing verb.
