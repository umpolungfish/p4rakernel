Land the 49 primitive-type kernel files, aligned to scripture

Each of the 49 primitive value types now has its own Lean file under
Imscribing/Primitives/Types/, registered in the lakefile: surface tokens,
the Belnap FOUR register descriptions, the full staged Imscription path
as an IGProtocol term, a kernel-proved Frobenius self-fusion theorem, and
a kernel-computed tier. These build green.

Before committing I checked their family/ordinal metadata against the
only authority, the pre-existing inductive constructor order in
Primitives/Core.lean, and several had drifted. The content of each file
(surface tokens, tuples, theorems) was already right; only the trailing
_family and _ordinal labels disagreed, so the fix is label-only:

  air  -> KineticChar 4     loll -> KineticChar 1   (were swapped)
  peep -> Fidelity 2        they -> Fidelity 1
  nun  -> Polarity 3        or   -> Polarity 4       yew -> Polarity 1
  ice  -> Granularity 2     so   -> Stoichiometry 1

ha_ha (Criticality 4) was already correct. After the fix every file
agrees with Core.lean. The same lakefile edit also registers
Imscribing.LiminalSpaces, included so no listed module dangles.
