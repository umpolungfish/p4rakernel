SIC_D12_Embedding: phi_rmul PROVEN. phi_foldl_inner/phi_foldl_outer distribute phi over the rmul double fold via contrib_sound + monVal_product + phi_foldl_insertAdd; per CL8NK register reading, phi_rmul restated on the canonical-key domain (all keys < 128) where the single-step reducers are sound - the general wild-key statement is false, and every frozen vector + contrib output is canonical, so the domain is the ring own granularity. Remaining: phi_rconj, norm_sq_eq_one, equiangular + 3 O0 boundary sorries.
