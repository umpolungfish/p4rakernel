Imscribing/OddRingIndex.lean: the index of an odd ring, machine-checked.

I took tonight's measured rings into the kernel. The carved macrocycle
[[0,2,1],[2,0,1],[1,1,0]] is congruent over Q to diag(4,-1,-1) by an explicit
change of basis (Sylvester, no spectral theorem): index n+ - n- = -1. C3 the
same, -1. The Void's ring C4 is congruent to diag(2,-1/2,0,0): index 0 —
perfect pairing, total cancellation. odd_survives_even_cancels states the
survival theorem. S4 ties on the measured face over R: charpoly x^3-6x-4,
factorization (x+2)(x^2-2x-2), spectrum exactly {-2, 1-sqrt 3, 1+sqrt 3},
gap sqrt(3)-1. Companion to HowDiracEquationArise S7 (Omega=ah forces the
integer index): this file computes that integer for the rings.

Axiom audit: standard trio; native_decide pair only on the decide lemmas.
Registered in lakefile globs. Full library green, 8452 jobs.
