Formalize the chem↔math isomorphism in the kernel

MoDoT now prints each structural operation in both registers; this proves
the identity behind that. Imscribing/Isomorphisms.lean shows, for four
verbs, that the chemical law and the mathematical law are the SAME Lean
proposition, holding by the kernel's existing μ∘δ=id theorems:

  click   self-fusion is idempotent  ==  μ∘δ = id on the diagonal
          (click_chem_iff_math is Iff.rfl — one theorem, two names)
  cycle   catalyst returns unchanged ==  a fixed point of μ∘δ
          (cycle_is_click: turnover IS self-fusion, by rfl)
  cleave  fusing fragments restores the bond == δ is a section of μ
  excite  re-exciting does nothing   ==  projection onto exceptional-point
          criticality err is idempotent (excite_idem by rfl); excite touches
          only Criticality (excite_preserves_off_crit)

Everything is grounded in Imscribing.Frobenius (δ_A, μ_A, mu_delta_A_id)
and the Core Criticality axis — no new axioms. The prose glosses MoDoT
emits are recorded beside the proofs as TwoFaces data so the string key
stays in sync with the proved content. Registered in the lakefile;
`lake build Imscribing.Isomorphisms` is green.
