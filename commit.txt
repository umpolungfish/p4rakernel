Manuscript sic_povm_stark_hilbert12: correct the d=12 coordinate field + add the
ramified S-unit double-cover subsection (2026-07-03)

Corrections (now gate-verified wrong in the old text):
- Moduli subfield degree 32 -> 16. The moduli |v_12(k)|^2 are REAL, so they generate the
  maximal real subfield (deg 16) of the conductor-(12) ray class field, not the deg-32
  field itself (which carries i). Fixed by two independent primitive elements, each with
  minimal polynomial of degree exactly 16.
- "coordinates lie in subfields of K_12 of degree dividing 288" -> the crux coordinates
  do NOT: v_12(1) is irreducible of degree 64 = 2^6, and 2^6 does not divide 288.

Advance (new subsection 7.5 "The coordinate field is a ramified S-unit double cover"):
- 8000-digit deep-vanish floor test: v_12(1) degree 64 irreducible => not in K_12; the
  coordinates generate a proper extension.
- v_12(1) = |v_12(1)| u_1 ; u_1 (phase) deg 32 in K_12 ; v_12(1)^2 = N_1 u_1^2 deg 32 in
  K_12 ; so v_12(1) = sqrt(v_12(1)^2) is a ramified quadratic double cover (one factor of
  2 above the 2^5 part of K_12).
- Ramifying class [N_1] is an S-UNIT class: N(N_1) = 9/9475854336 = 1/32448^2, and
  32448 = 2^6*3*13^2 is supported only on the ramified primes {2,3,13} (disc 2^12 3^2 13^4).
  So K_12(sqrt N_1)/K_12 ramifies only where K_12 does; S = {2,3,13}.
- This RESOLVES an internal inconsistency: the construction v_d(k)=sigma_k(eps_d) with
  eps_d in K_d forces degree|288, contradicting deg 64. The exact construction is
  v_d(k) = sigma_k(sqrt eps), the embedding of the SQUARE ROOT of the Stark datum =>
  64 = 2*32. This is the arithmetic shadow of the dual-link self-pairing: N_k = v*conj(v)
  is the idempotent collapse into the real subfield; the coordinate is its sqrt double cover.
- Scope updated: K_12 is generated by the phase+modulus data; the full coordinate field is
  the double cover K_12(sqrt N_k). Remaining: pin the exact S-unit representative.

Compiled clean (lualatex, 2 passes, 0 errors, no undefined refs); PDF copied to
pdfs/sic_povm_stark_hilbert12.pdf.
