Shavian notation migration + explicit Belnap/Majorana/SIC iso witness

Two changes, one commit theme: say what the tree means in its current
language, and say the isomorphism instead of gesturing at it.

Notation: all old-era primitive IDs in comments and docstrings are migrated
to the NSNS_PRIME.md Shavian glyphs, both the glyph-pair code IDs (D_omega
era) and the older human-name forms (Omega_0, Phi_c, D_infty, T_bowtie era).
Comment-only sweep, 790 tokens across 48 files; live code strings
(GeneToProtein codon tables, CLU_GPM promotion data, FrobeniusUnification
ruleset names) and genuine math notation (cosmology density parameters in
CosmologyBelnap, summation indices) are untouched. One pre-existing
ID/label mismatch fixed in BanachMeasure: the chirality promotion target
read H2 but was labeled wool; the Stone's chirality is wool.

FrobeniusIso.lean: MajoranaFixed proved three fixed-point facts side by
side; the one-object claim needs morphism data, not a conjunction. The new
module supplies it: orbitalBelnapEquiv (explicit toFun/invFun/left_inv/
right_inv), the transported operations (pair=join, depair=belnapDepair,
spinFlip=bnot, omeet=meet), naturality for all four, transport of the
Frobenius identity and the SIC equiangularity, and the dialetheic fixed
point carried to the dialetheic fixed point. belnap_majorana_sic_iso
bundles the witness. Builds green, zero sorries, no native_decide.
