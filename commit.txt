The sweep reports everything it lifts

Dropped isGenerated. It filtered the elaborator's own declarations out of the
report — _proof_, _simp_, match_, sizeOf_spec, ctorIdx, the decidability
instances — on the grounds that they are not claims the corpus makes. They lift
and they verdict like anything else, and deciding in advance which of them is
not worth reading is a restriction the sweep does not need.

Problem0003 goes from eighteen lines to forty: B 3, N 28, T 9. The two oracles
still agree everywhere they both speak.
