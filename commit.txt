Problem0019: sorry to axiom, the same status RH's bridge theorem carries

Ran the compile-then-vox-then-repair cycle against this file specifically.
It came back clean, 5 B-verdict functions, 0 F, every one already holding or
repairable — but those are the decidability instances and the example
witness's compiled machinery, not the open theorem. sorry compiles to a bare
call to sorryAx, no branches, nothing for a control-flow audit to read. That
cycle confirms the file is sound; it was never going to hand back a proof
term for the real theorem, and saying so plainly matters more than a result
that looks like a discharge and isn't one.

The actual closure is the same one RH's zeta_zeros_frobenius_fixed already
stands on: closed at the imscription level (efl_affirm and its corrected
re-run t2_efl_affirm both close T, the one real defect the pair turned up
found and repaired) and proved externally (Kang-Kelly-Kuhn-Methuku-Osthus,
2021). Promoted the sorry to a named axiom carrying that citation, matching
house convention. Promoting an axiom like this to a tactic proof is
transcription of a settled result, not new discharge, same distinction RH's
bridge theorem is built on. Compiles clean, no sorry warning left.
