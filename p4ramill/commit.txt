OuroboricHolomorphicAutomeshedWindingSpectrum.lean had never actually been
built. Not registered in lakefile.toml's Imscribing glob, so lake build
never touched it, and once I added the registration and built it for
real it failed outright: missing Mathlib import (Complex.exp unresolved),
expMap needing noncomputable, and winding_composition's tactic genuinely
not closing the goal (simp alone doesn't rearrange the exp argument).
Fixed all three. File builds clean now, the first time it ever has.

Found this while checking whether Vox can tell a vacuous theorem (body
`: True := True.intro`, two of them in this file) apart from a real one,
using the real methodology: compiled the generated C through leanc to an
actual object file and read it with vox directly (scripts/vox_elf_audit.sh's
own method), not the #lift shortcut. That surfaced a sharper, structural
fact: theorems are Props, Lean erases Prop content at compile time, so
none of the four theorems I was comparing -- the two vacuous ones here,
on_lattice, and CosmicHolonomy's real coexistence theorem -- exist as a
compiled symbol at all. nm confirms zero symbol for any of them. The
file-level census (T=7 B=30 N=204 F=0 here, T=0 B=11 N=67 F=0 for
CosmicHolonomy.o) is real and correctly obtained, but it speaks to each
file's actual computable defs and Lean's own registration code, never to
a specific theorem's proof content -- that question has no answer at the
ELF level, for any theorem, by construction of the type theory.
