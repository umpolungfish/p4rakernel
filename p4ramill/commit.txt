Defined the paraconsistent witness type. Not P and its negation for one
fixed P -- that's not buildable in Lean without the whole environment
going unsound, h2 h1 : False, and once False is inhabited every Prop is,
whether or not the resulting term ever gets pattern matched into
False.elim. Consistency is a property of the whole kernel, not one local
site. What's real and sound: Verdict P : Type, four constructors,
proved/refuted/held/admitted, where held carries two INDEPENDENTLY real
facts about the same object, P and a genuinely different Q, not P and
not-P. That's the actual shape CosmicHolonomy's coexistence theorem and
ENGAGR both already have.

Type, not Prop, on purpose: a Prop's content is erased by Lean's
compiler at every use, confirmed directly earlier today with nm on real
compiled objects, zero symbol for any theorem's proof, real or vacuous.
Verdict P doesn't erase -- which constructor a value uses is real,
compiled data, so a function dispatching on it produces real machine
code Vox can audit, closing the exact gap the ELF-audit thread found.
Checked, not assumed: compiled DialetheicWitness.lean through leanc same
as scripts/vox_elf_audit.sh does, classify and engagrVerdict both
survive as real symbols (nm confirms), engagrVerdict_is_B does not
(it's a theorem, erased, exactly as predicted). vox on the real object:
T=0 B=10 N=181 F=0, no explosion at the machine-code level either.

Added Reg16_3.engagr to SixteenThreeTrilattice.lean first -- it only
existed in Rust (added there earlier today), never in Lean. Mirrors the
Rust definition exactly, proved sound (engagr_holds_both, by decide),
and is the file's own worked instance of `held`: for any register,
engagr's smallT and smallF are two independently real, decide-proved
facts, not one proposition and its negation. #print axioms confirms the
distinction from sorry mechanically: anAdmittedVerdict depends on no
axioms at all, engagrVerdict_is_B depends only on the three standard
foundational axioms every classical Mathlib proof cites (propext,
Classical.choice, Quot.sound) -- never sorryAx.
