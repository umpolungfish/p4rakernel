Unify the Belnap carrier, holding both conjunctions

Declare the Belnap carrier once and let it hold both conjunctions, rather than
picking a winner. The canonical Imscribing.Paraconsistent.Belnap now carries band,
the FDE truth-meet (N ∧ B = F), and band_dom, the dominance conjunction
(N ∧ B = N), with bor and bor_dom to match. Two instruments on one carrier, both
true, neither privileged.

Moved the last three holders of the standalone carrier onto the canonical one:
ParaconsistentFrobeniusClosure, ClassicalRestriction, DeMorganBooleanCentre. None
leaned on the dominance behaviour semantically. DeMorgan defines its own truth-meet,
the Frobenius closure inc collapses every value to B regardless of the conjunction,
and the classical fragment uses only negation. The dominance references in
ParaconsistentFrobeniusClosure now point at band_dom, so the operator is preserved,
not lost.

Deleted the duplicate carrier ParaconsistentCore.lean and dropped it from the
lakefile. IUTT had reached the root Belnap transitively; gave it the canonical open.
The whole downstream builds green: the three files, IUTT, IUTT_Audit, ABC, the
ThetaBridge modules. Sans sorry.

CircumPunctum was already moved in the previous commit; this finishes the
unification. One carrier, both bands held.

Whole-tree snapshot.
