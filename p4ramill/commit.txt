Extend the dialetheic verdict to Riemann, the dialetheic universe, and the ambient

Continue applying DialetheicWitness's held verdict to the genuine both-states, in
value order.

  - RiemannSIC: the ground imscription holds Frobenius closure (μ∘δ = id) together
    with the Hilbert-Pólya spectral correspondence, the zeta-zero and the
    Hamiltonian-eigenvalue readings of one object. riemannDialetheiaVerdict holds
    both; classify reads B.
  - DialetheicUniverse: the frontier state B is self-dual under negation (¬B = B)
    and invariant under double negation (¬¬B = B). universeDialetheiaVerdict holds
    both; classify reads B.
  - CircumPunctum: the ambient B is the fixed point of the closure (Inc B = B) and
    unreachable from the Boolean core (the Boolean impossibility). Stable inside,
    unreachable from outside. ambientBVerdict holds both; classify reads B.

Each classify = B by rfl. All three files build green, and CircumPunctum takes the
witness without the two-Belnap drift causing any ambiguity, since its open is the
root Belnap and the witness carrier stays fully qualified.

Whole-tree snapshot.
