Apply the dialetheic verdict to the Gödel sentence and the self-verifying kernel

DialetheicWitness's `held` verdict fits any site where two independently real facts
about one object are held together though they look opposed. The classifier reads
that as B, and unlike a Prop it compiles, so Vox can audit the both-state. It was
already applied across the ABC and Millennium theta-bridges. Two strong sites had
genuine both-states and no witness.

  - GodelCompleteSentence: the Gödel-complete sentence is both true and false
    (join T F = B) and stable under negation (bnot B = B). godelSentenceVerdict
    holds the two, and godelSentenceVerdict_is_B reads it B by rfl.
  - SelfVerification: the self-verifying kernel's register is held at B and is
    neither pole (≠ F and ≠ T), the pair that makes it a sustained paraconsistent
    state rather than a collapse. kernelBothVerdict holds them; classify reads B.

Both files build green.

Further genuine both-state sites, grounded, not yet witnessed: RiemannSIC
(dialetheia_structural), DialetheicUniverse (dialetheia_is_stable, B_double_neg),
CircumPunctum (the ambient B fixed point with no explosion), and the horn-torus
pinch. Named for the next pass.

Whole-tree snapshot.
