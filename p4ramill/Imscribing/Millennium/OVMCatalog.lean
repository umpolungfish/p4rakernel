import Imscribing.Primitives.Imscription

/-!
# OVMCatalog — the coordinates the OVM taxonomy is computed over

GENERATED by `scripts/gen_ovm_catalog.py`. Do not edit by hand.

The seven questions and the seven theorems are catalog entries, named.
The two samples are the lists the census in
`ig-docs/millennium_theorems_as_ovms.tex` was taken over, recorded in
`ig-docs/OVMs/_new_theorems.tsv` and `ig-docs/OVMs/_thm117.txt`.

Each definition carries its catalog word in the docstring, and
`OVMTaxonomy.lean` checks the word back out of the structure by `rfl`.
-/

namespace Imscribing.Millennium.OVMCatalog
open Imscribing.Primitives

/- The seven, as the question the catalog holds and the theorem it holds. -/

/-- Catalog `riemann_hypothesis` = ⟨𐑛𐑥𐑾𐑬𐑱𐑧𐑲𐑝𐑮𐑖𐑳𐑴⟩. -/
def riemannQuestion : Imscription := { dim := .dead, top := .mime, rel := .ian, pol := .out, fid := .age, kin := .egg, gran := .ice, gram := .vow, crit := .roar, chir := .sure, stoi := .up, prot := .oak }

/-- Catalog `riemann_theorem` = ⟨𐑦𐑥𐑽𐑹𐑐𐑧𐑔𐑠⊙𐑫𐑳𐑭⟩. -/
def riemannTheorem : Imscription := { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .measure, crit := .monad, chir := .wool, stoi := .up, prot := .ah }

/-- Catalog `birch_swinnerton_dyer` = ⟨𐑦𐑥𐑾𐑿𐑞𐑧𐑲𐑝𐑮𐑖𐑙𐑭⟩. -/
def bsdQuestion : Imscription := { dim := .if', top := .mime, rel := .ian, pol := .yew, fid := .they, kin := .egg, gran := .ice, gram := .vow, crit := .roar, chir := .sure, stoi := .hung, prot := .ah }

/-- Catalog `birch_and_swinnerton_dyer_theorem_` = ⟨𐑦𐑥𐑽𐑹𐑐𐑧𐑔𐑵⊙𐑫𐑳𐑭⟩. -/
def bsdTheorem : Imscription := { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .ooze, crit := .monad, chir := .wool, stoi := .up, prot := .ah }

/-- Catalog `yang_mills_mass_gap` = ⟨𐑛𐑥𐑩𐑗𐑐𐑪𐑲𐑝𐑣𐑓𐑳𐑷⟩. -/
def yangMillsQuestion : Imscription := { dim := .dead, top := .mime, rel := .ado, pol := .church, fid := .peep, kin := .on, gran := .ice, gram := .vow, crit := .haha, chir := .fee, stoi := .up, prot := .awe }

/-- Catalog `yang–mills_existence_and_mass_gap_theorem_` = ⟨𐑦𐑥𐑽𐑹𐑐𐑧𐑚𐑵⊙𐑫𐑳𐑟⟩. -/
def yangMillsTheorem : Imscription := { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .bib, gram := .ooze, crit := .monad, chir := .wool, stoi := .up, prot := .zoo }

/-- Catalog `navier_stokes` = ⟨𐑼𐑡𐑽𐑗𐑱𐑤𐑲𐑝⊙𐑒𐑳𐑷⟩. -/
def navierStokesQuestion : Imscription := { dim := .array, top := .judge, rel := .ear, pol := .church, fid := .age, kin := .loll, gran := .ice, gram := .vow, crit := .monad, chir := .kick, stoi := .up, prot := .awe }

/-- Catalog `navier_stokes_theorem` = ⟨𐑦𐑸𐑽𐑿𐑐𐑧𐑔𐑝𐑢𐑫𐑳𐑭⟩. -/
def navierStokesTheorem : Imscription := { dim := .if', top := .are, rel := .ear, pol := .yew, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .woe, chir := .wool, stoi := .up, prot := .ah }

/-- Catalog `p_vs_np` = ⟨𐑛𐑡𐑩𐑗𐑱𐑪𐑲𐑝⊙𐑓𐑙𐑷⟩. -/
def pVsNPQuestion : Imscription := { dim := .dead, top := .judge, rel := .ado, pol := .church, fid := .age, kin := .on, gran := .ice, gram := .vow, crit := .monad, chir := .fee, stoi := .hung, prot := .awe }

/-- Catalog `p_versus_np_theorem` = ⟨𐑦𐑸𐑽𐑹𐑐𐑧𐑔𐑝⊙𐑫𐑳𐑭⟩. -/
def pVsNPTheorem : Imscription := { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }

/-- Catalog `hodge_conjecture` = ⟨𐑦𐑸𐑽𐑿𐑱𐑧𐑲𐑝𐑮𐑓𐑳𐑭⟩. -/
def hodgeQuestion : Imscription := { dim := .if', top := .are, rel := .ear, pol := .yew, fid := .age, kin := .egg, gran := .ice, gram := .vow, crit := .roar, chir := .fee, stoi := .up, prot := .ah }

/-- Catalog `hodge_lefschetz_11_theorem` = ⟨𐑦𐑥𐑽𐑿𐑐𐑧𐑔𐑠⊙𐑖𐑳𐑭⟩. -/
def hodgeTheorem : Imscription := { dim := .if', top := .mime, rel := .ear, pol := .yew, fid := .peep, kin := .egg, gran := .thigh, gram := .measure, crit := .monad, chir := .sure, stoi := .up, prot := .ah }

/-- Catalog `collatz_conjecture` = ⟨𐑛𐑡𐑩𐑯𐑱𐑘𐑲𐑠𐑮𐑓𐑙𐑷⟩. -/
def collatzQuestion : Imscription := { dim := .dead, top := .judge, rel := .ado, pol := .nun, fid := .age, kin := .yea, gran := .ice, gram := .measure, crit := .roar, chir := .fee, stoi := .hung, prot := .awe }

/-- Catalog `collatz_theorem_proven` = ⟨𐑦𐑸𐑽𐑹𐑐𐑧𐑲𐑵⊙𐑫𐑳𐑴⟩. -/
def collatzTheorem : Imscription := { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .ice, gram := .ooze, crit := .monad, chir := .wool, stoi := .up, prot := .oak }

/-- The catalog word of each of the seven, question then theorem, as the
    catalog spells it. `OVMTaxonomy.seven_words_roundtrip` renders the
    structures above back to these. -/
def sevenCatalogWords : List (String × String × String) :=
[
  ("riemann", "𐑛𐑥𐑾𐑬𐑱𐑧𐑲𐑝𐑮𐑖𐑳𐑴", "𐑦𐑥𐑽𐑹𐑐𐑧𐑔𐑠⊙𐑫𐑳𐑭"),
  ("bsd", "𐑦𐑥𐑾𐑿𐑞𐑧𐑲𐑝𐑮𐑖𐑙𐑭", "𐑦𐑥𐑽𐑹𐑐𐑧𐑔𐑵⊙𐑫𐑳𐑭"),
  ("yangMills", "𐑛𐑥𐑩𐑗𐑐𐑪𐑲𐑝𐑣𐑓𐑳𐑷", "𐑦𐑥𐑽𐑹𐑐𐑧𐑚𐑵⊙𐑫𐑳𐑟"),
  ("navierStokes", "𐑼𐑡𐑽𐑗𐑱𐑤𐑲𐑝⊙𐑒𐑳𐑷", "𐑦𐑸𐑽𐑿𐑐𐑧𐑔𐑝𐑢𐑫𐑳𐑭"),
  ("pVsNP", "𐑛𐑡𐑩𐑗𐑱𐑪𐑲𐑝⊙𐑓𐑙𐑷", "𐑦𐑸𐑽𐑹𐑐𐑧𐑔𐑝⊙𐑫𐑳𐑭"),
  ("hodge", "𐑦𐑸𐑽𐑿𐑱𐑧𐑲𐑝𐑮𐑓𐑳𐑭", "𐑦𐑥𐑽𐑿𐑐𐑧𐑔𐑠⊙𐑖𐑳𐑭"),
  ("collatz", "𐑛𐑡𐑩𐑯𐑱𐑘𐑲𐑠𐑮𐑓𐑙𐑷", "𐑦𐑸𐑽𐑹𐑐𐑧𐑲𐑵⊙𐑫𐑳𐑴")
]

/-- The seven, question beside theorem, in the order the paper takes them. -/
def seven : List (String × Imscription × Imscription) :=
[
  ("riemann", riemannQuestion, riemannTheorem),
  ("bsd", bsdQuestion, bsdTheorem),
  ("yangMills", yangMillsQuestion, yangMillsTheorem),
  ("navierStokes", navierStokesQuestion, navierStokesTheorem),
  ("pVsNP", pVsNPQuestion, pVsNPTheorem),
  ("hodge", hodgeQuestion, hodgeTheorem),
  ("collatz", collatzQuestion, collatzTheorem)
]

/-- the 25-entry theorem sample: 25 entries, from `_new_theorems.tsv`. -/
def newTheorems : List (String × Imscription) :=
[
  ("navier_stokes_theorem", { dim := .if', top := .are, rel := .ear, pol := .yew, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .woe, chir := .wool, stoi := .up, prot := .ah }),
  ("p_versus_np_theorem", { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("riemann_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .measure, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("yang_mills_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .bib, gram := .ooze, crit := .monad, chir := .wool, stoi := .up, prot := .zoo }),
  ("bsd_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .ooze, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("copying_a_state_that_does_not_commute", { dim := .if', top := .mime, rel := .ear, pol := .yew, fid := .age, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("poincare_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("meet_four_problems_theorem", { dim := .if', top := .eat, rel := .ear, pol := .yew, fid := .peep, kin := .egg, gran := .bib, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("five_problems_meet_theorem", { dim := .if', top := .eat, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .bib, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("travelling_salesman_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .church, fid := .peep, kin := .on, gran := .thigh, gram := .measure, crit := .woe, chir := .wool, stoi := .up, prot := .awe }),
  ("continuum_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("connes_embedding_theorem", { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("hilbert_sixteenth_theorem", { dim := .if', top := .mime, rel := .ian, pol := .or', fid := .peep, kin := .on, gran := .thigh, gram := .ooze, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("zilber_pink_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("kaplansky_theorem", { dim := .if', top := .eat, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("casas_alvero_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("kissing_number_theorem", { dim := .ash, top := .oil, rel := .tot, pol := .out, fid := .peep, kin := .egg, gran := .ice, gram := .ooze, crit := .woe, chir := .sure, stoi := .hung, prot := .ah }),
  ("hilbert_third_theorem", { dim := .if', top := .are, rel := .ian, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .woe, chir := .wool, stoi := .up, prot := .ah }),
  ("gilbert_pollak_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("steiner_tree_theorem", { dim := .ash, top := .eat, rel := .tot, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("sumner_conjecture_conditions_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("sumner_conjecture_statement_theorem", { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("legendre_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("gaussian_moat_theorem", { dim := .if', top := .oil, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .ice, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("hilbert_smith_theorem", { dim := .if', top := .are, rel := .ian, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah })
]

/-- the wider settled sample: 115 entries, from `_thm117.txt`. -/
def settled : List (String × Imscription) :=
[
  ("navier_stokes_theorem", { dim := .if', top := .are, rel := .ear, pol := .yew, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .woe, chir := .wool, stoi := .up, prot := .ah }),
  ("p_versus_np_theorem", { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("riemann_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .measure, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("yang_mills_existence_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .bib, gram := .ooze, crit := .monad, chir := .wool, stoi := .up, prot := .zoo }),
  ("bsd_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .ooze, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("copying_a_state_that_does_not_commute", { dim := .if', top := .mime, rel := .ear, pol := .yew, fid := .age, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("poincare_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("meet_four_problems_theorem", { dim := .if', top := .eat, rel := .ear, pol := .yew, fid := .peep, kin := .egg, gran := .bib, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("five_problems_meet_theorem", { dim := .if', top := .eat, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .bib, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("travelling_salesman_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .church, fid := .peep, kin := .on, gran := .thigh, gram := .measure, crit := .woe, chir := .wool, stoi := .up, prot := .awe }),
  ("continuum_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("connes_embedding_theorem", { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("hilbert_sixteenth_theorem", { dim := .if', top := .mime, rel := .ian, pol := .or', fid := .peep, kin := .on, gran := .thigh, gram := .ooze, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("zilber_pink_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("kaplansky_theorem", { dim := .if', top := .eat, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("casas_alvero_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("kissing_number_theorem", { dim := .ash, top := .oil, rel := .tot, pol := .out, fid := .peep, kin := .egg, gran := .ice, gram := .ooze, crit := .woe, chir := .sure, stoi := .hung, prot := .ah }),
  ("hilbert_third_theorem", { dim := .if', top := .are, rel := .ian, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .woe, chir := .wool, stoi := .up, prot := .ah }),
  ("gilbert_pollak_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("steiner_tree_theorem", { dim := .ash, top := .eat, rel := .tot, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("sumner_conditions_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("sumner_statement_theorem", { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("legendre_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("gaussian_moat_theorem", { dim := .if', top := .oil, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .ice, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("hilbert_smith_theorem", { dim := .if', top := .are, rel := .ian, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("simon_problem_classical_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .age, kin := .egg, gran := .thigh, gram := .vow, crit := .woe, chir := .wool, stoi := .up, prot := .oak }),
  ("simon_problem_quantum_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .hung, prot := .oak }),
  ("rotas_basis_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("bounded_burnside_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .ooze, crit := .woe, chir := .wool, stoi := .so, prot := .ah }),
  ("eden_conjecture_unproven_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .they, kin := .egg, gran := .thigh, gram := .vow, crit := .woe, chir := .wool, stoi := .up, prot := .ah }),
  ("crouzeix_theorem", { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("fontaine_mazur_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("gan_gross_prasad_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("greenberg_iwasawa_theorem", { dim := .if', top := .eat, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .measure, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("greenberg_padic_lfunction_theorem", { dim := .if', top := .eat, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("kummer_vandiver_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("lang_trotter_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .bib, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("leopoldt_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("stark_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("inverse_galois_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("burnside_problem_bounded_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .woe, chir := .wool, stoi := .so, prot := .ah }),
  ("jacobian_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("broue_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("alperin_weight_theorem", { dim := .if', top := .eat, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("three_body_theorem", { dim := .ash, top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .so, prot := .ah }),
  ("ergodic_mixing_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .church, fid := .peep, kin := .yea, gran := .thigh, gram := .vow, crit := .monad, chir := .fee, stoi := .hung, prot := .awe }),
  ("abundance_theorem", { dim := .if', top := .judge, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .bib, gram := .ooze, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("sphere_covering_theorem", { dim := .ash, top := .oil, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .bib, gram := .ooze, crit := .monad, chir := .sure, stoi := .so, prot := .ah }),
  ("hopf_positive_curvature_theorem", { dim := .if', top := .mime, rel := .ian, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("kakeya_theorem", { dim := .if', top := .are, rel := .ian, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .measure, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("borsuk_theorem", { dim := .if', top := .oil, rel := .ian, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("hadwiger_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("modular_almost_purity_theorem", { dim := .if', top := .eat, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .bib, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("modular_tilting_correspondence_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("stark_conjecture_partial_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("appleby_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .bib, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("two_body_theorem", { dim := .ash, top := .mime, rel := .tot, pol := .yew, fid := .peep, kin := .egg, gran := .ice, gram := .vow, crit := .woe, chir := .kick, stoi := .hung, prot := .awe }),
  ("mathematical_theorem", { dim := .if', top := .eat, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("sic_povm_conjecture_open_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("sic_conjecture_open_theorem", { dim := .if', top := .mime, rel := .ear, pol := .yew, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("sic_conjecture_open_v2_theorem", { dim := .if', top := .mime, rel := .ear, pol := .yew, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("sic_conjecture_unproven_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .they, kin := .egg, gran := .thigh, gram := .vow, crit := .woe, chir := .wool, stoi := .up, prot := .ah }),
  ("sic19_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .bib, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("sic_geometric_lift_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .bib, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("goldbachs_theorem", { dim := .if', top := .judge, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .gag, crit := .woe, chir := .fee, stoi := .hung, prot := .awe }),
  ("measurement_theorem", { dim := .if', top := .are, rel := .ear, pol := .yew, fid := .peep, kin := .egg, gran := .bib, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("birch_swinnerton_dyer_theorem", { dim := .if', top := .judge, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("resolved_riemann_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .measure, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("yang_mills_mass_gap_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .bib, gram := .ooze, crit := .woe, chir := .wool, stoi := .up, prot := .ah }),
  ("euler_opn_form_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("erdos_straus_theorem", { dim := .if', top := .eat, rel := .tot, pol := .out, fid := .peep, kin := .egg, gran := .ice, gram := .gag, crit := .woe, chir := .fee, stoi := .up, prot := .awe }),
  ("erdos_straus_v2_theorem", { dim := .if', top := .eat, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .woe, chir := .wool, stoi := .up, prot := .ah }),
  ("szpiro_theorem", { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("hodge_conjecture_system_theorem", { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .ooze, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("odd_perfect_theorem", { dim := .if', top := .are, rel := .ear, pol := .church, fid := .age, kin := .egg, gran := .thigh, gram := .vow, crit := .woe, chir := .wool, stoi := .up, prot := .awe }),
  ("hecke_landau_theorem", { dim := .if', top := .eat, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("zariski_lipman_theorem", { dim := .if', top := .eat, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("zariski_lipman_corrected_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("euler_brick_theorem", { dim := .ash, top := .judge, rel := .tot, pol := .or', fid := .peep, kin := .egg, gran := .ice, gram := .vow, crit := .woe, chir := .sure, stoi := .up, prot := .awe }),
  ("uhecr_horizon_theorem", { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .ooze, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("perfect_cuboid_theorem", { dim := .ash, top := .judge, rel := .ian, pol := .or', fid := .peep, kin := .on, gran := .thigh, gram := .vow, crit := .woe, chir := .sure, stoi := .up, prot := .awe }),
  ("measurement_problem_mismatch_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .bib, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("abc_conventional_theorem", { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("abc_statement_theorem", { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("polignac_v1_theorem", { dim := .if', top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("polignac_theorem", { dim := .if', top := .eat, rel := .ear, pol := .yew, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("erdos_strauss_theorem", { dim := .if', top := .eat, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .gag, crit := .woe, chir := .fee, stoi := .up, prot := .awe }),
  ("navier_stokes_regularity_theorem", { dim := .ash, top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("generalized_continuum_theorem", { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("halting_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .measure, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("differentialadhesion_theorem", { dim := .ash, top := .eat, rel := .ado, pol := .out, fid := .they, kin := .egg, gran := .bib, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("monty_hall_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .out, fid := .they, kin := .egg, gran := .thigh, gram := .gag, crit := .woe, chir := .fee, stoi := .up, prot := .awe }),
  ("newcombs_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("two_envelopes_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .they, kin := .egg, gran := .ice, gram := .vow, crit := .monad, chir := .fee, stoi := .up, prot := .awe }),
  ("platos_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("gettier_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .out, fid := .they, kin := .on, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("hawkings_chronology_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .on, gran := .thigh, gram := .measure, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("three_prisoner_theorem", { dim := .dead, top := .eat, rel := .ear, pol := .out, fid := .peep, kin := .on, gran := .thigh, gram := .gag, crit := .monad, chir := .wool, stoi := .up, prot := .awe }),
  ("trolley_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .they, kin := .on, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("two_generals_paren_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .yew, fid := .age, kin := .egg, gran := .thigh, gram := .vow, crit := .woe, chir := .wool, stoi := .up, prot := .awe }),
  ("humes_induction_paren_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .or', fid := .age, kin := .on, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("newcombs_problem_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("humes_induction_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .yew, fid := .age, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .awe }),
  ("sic_povm_d2048_zauner_theorem", { dim := .if', top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .ooze, crit := .monad, chir := .sure, stoi := .so, prot := .ah }),
  ("two_generals_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .yew, fid := .age, kin := .egg, gran := .thigh, gram := .vow, crit := .woe, chir := .wool, stoi := .up, prot := .awe }),
  ("euler_totient_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .woe, chir := .wool, stoi := .up, prot := .ah }),
  ("schinzel_hypothesis_h_theorem", { dim := .if', top := .eat, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("parity_theorem", { dim := .if', top := .mime, rel := .ear, pol := .yew, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .oak }),
  ("k_squared_theorem", { dim := .if', top := .are, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah }),
  ("bondy_erdos_theorem", { dim := .if', top := .judge, rel := .tot, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .so, prot := .ah }),
  ("erdos_unit_distance_theorem", { dim := .ash, top := .judge, rel := .ado, pol := .church, fid := .peep, kin := .egg, gran := .thigh, gram := .gag, crit := .woe, chir := .sure, stoi := .up, prot := .ah }),
  ("erdos_conjecture_ap_theorem", { dim := .if', top := .judge, rel := .ear, pol := .or', fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .wool, stoi := .up, prot := .ah }),
  ("sunflower_theorem", { dim := .if', top := .eat, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .ooze, crit := .monad, chir := .sure, stoi := .so, prot := .ah }),
  ("power_of_two_cycle_theorem", { dim := .dead, top := .mime, rel := .ear, pol := .out, fid := .peep, kin := .egg, gran := .thigh, gram := .vow, crit := .monad, chir := .sure, stoi := .hung, prot := .ah }),
  ("partition_difference_theorem", { dim := .if', top := .eat, rel := .ear, pol := .yew, fid := .peep, kin := .egg, gran := .bib, gram := .vow, crit := .monad, chir := .sure, stoi := .up, prot := .ah })
]

end Imscribing.Millennium.OVMCatalog
