A bound on the weighted cross sum of one tree level, valid once the level exceeds a stated size, small enough that three quarters plus it stays below one. It must come from the residue structure and not from the census.
