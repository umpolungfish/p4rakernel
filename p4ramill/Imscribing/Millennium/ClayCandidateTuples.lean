import Imscribing.Primitives.Imscription

/-! # ClayCandidateTuples — candidate transport targets, procedurally sourced
   from IG_catalog.json via the same GLYPH inverse-map as ClayCanonicalTuples.
   Not part of the canonical six; exploratory hop targets. -/

namespace Imscribing.Millennium.ClayCandidateTuples
open Imscribing.Primitives
open Dimensionality Topology Relational Polarity Fidelity KineticChar
     Granularity Grammar Criticality Chirality Stoichiometry Protection

/-- Catalog `rh_ep_join`, procedurally sourced. -/
def rhEpJoin : Imscription := { dim := .if', top := .are, rel := .ear, pol := .or', fid := .peep, kin := .on, gran := .ice, gram := .vow, crit := .err, chir := .kick, stoi := .up, prot := .ah }

/-- Catalog `rh_hc_join`, procedurally sourced. -/
def rhHcJoin : Imscription := { dim := .if', top := .are, rel := .tot, pol := .or', fid := .peep, kin := .loll, gran := .ice, gram := .gag, crit := .roar, chir := .fee, stoi := .up, prot := .ah }

/-- Catalog `meet_rh_hc`, procedurally sourced. -/
def meetRhHc : Imscription := { dim := .if', top := .are, rel := .tot, pol := .nun, fid := .peep, kin := .loll, gran := .ice, gram := .vow, crit := .monad, chir := .fee, stoi := .so, prot := .ah }

end Imscribing.Millennium.ClayCandidateTuples
