-- Imscribing/VoxResidentGrammar.lean
-- Stage 24 (Grammar tie-in): the resident schedule word ∈∋⊤≻⊡ as an IGProtocol term.
--
-- The Vox resident REDUCE word is `∈ ∋ ⊤ ≻ ⊡` = FSPLIT · FFUSE · EVALT · AFWD · IFIX.
-- The scaffold below is the kernel's own reading of that word: fingerprint sig=(1,2,1,1),
-- Frobenius order 1, period 5, FSPLIT/FFUSE pair (0,1), tier O₂. The protocol term and
-- its Frobenius theorem are what this file adds to the frozen semantics.
--
-- Generated by proof_scaffold(ops=[FSPLIT,FFUSE,EVALT,AFWD,IFIX], name=voxResidentSchedule).

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def voxresidentschedule_s0 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def voxresidentschedule_s1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def voxresidentschedule_s2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def voxresidentschedule_s3 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def voxresidentschedule_s4 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def voxresidentschedule_l0 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def voxresidentschedule_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def voxresidentschedule_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def voxresidentschedule_l3 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def voxresidentschedule_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def voxresidentschedule_protocol :
    IGProtocol voxresidentschedule_s0 voxresidentschedule_s4 :=
  .withGram Grammar.measure <|
  (.seq (.prod
      (.arrow voxresidentschedule_l0 voxresidentschedule_s0 voxresidentschedule_s1)
      (.arrow voxresidentschedule_l0 voxresidentschedule_s0 voxresidentschedule_s1))
    (.seq (.arrow voxresidentschedule_l1 voxresidentschedule_s1 voxresidentschedule_s1)
      (.seq (.arrow voxresidentschedule_l1 voxresidentschedule_s1 voxresidentschedule_s2)
        (.seq (.arrow voxresidentschedule_l2 voxresidentschedule_s2 voxresidentschedule_s3)
          (.arrow voxresidentschedule_l3 voxresidentschedule_s3 voxresidentschedule_s4)))))

-- ── Evaluation arm sub-def ────────────────────────────────────
-- strict relation ≡s accepts the reduction
noncomputable def voxresidentschedule_true_arm :
    IGProtocol voxresidentschedule_s0 voxresidentschedule_s4 :=
  (voxresidentschedule_protocol).restrictToEVALT

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application).
def voxresidentschedule_tier_ground : OuroboricityTier :=
  TierFunctor.obj voxresidentschedule_s0
def voxresidentschedule_tier : OuroboricityTier :=
  TierFunctor.obj voxresidentschedule_s4
-- The kernel's verdict on the closed object is O₂ (frozen below as a decidable check).
example : voxresidentschedule_tier = OuroboricityTier.O₂ := by decide

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem voxresidentschedule_frobenius :
    igFrobeniusAlg.mul voxresidentschedule_s0 voxresidentschedule_s0 = voxresidentschedule_s0 :=
  igFrobAlg_self_fusion voxresidentschedule_s0

end Imscribing
