-- IGProtocol scaffold: ⊢ → ∈ → ≻ → ⊤ → ⋈ → ⊙ → ≺ → ⊥ → ⊞ → ∋ → ⊡ → ⋈ → ⊙ → ⊣
-- Class: Partition the domain into the constructive proof arm and the refutation arm.
-- Fingerprint: sig=(8,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=14
-- Expected tier: O₂dag
-- ∈/∋ pairs: [(1, 9)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] ⊢     dim    := 𐑼               𐑼 → 𐑚  | initial object — ground of distinction
--   [1] ∈    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [2] ≻      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [3] ⊤     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [4] ⋈     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [5] ⊙   gram   := 𐑠               𐑚 → 𐑙  | identity — self-imscription
--   [6] ≺      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [7] ⊥     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [8] ⊞    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [9] ∋     stoi   := 𐑙               𐑙 → 𐑭  | fuse μ — assembly mode
--   [10] ⊡      prot   := 𐑭               𐑙 → 𐑱  | irreversible fixation — winding number
--   [11] ⋈     fid    := 𐑱               𐑭 → 𐑠  | composition — regime coherence
--   [12] ⊙   gram   := 𐑠               𐑱 → 𐑡  | identity — self-imscription
--   [13] ⊣     top    := 𐑡               𐑠 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def partition_the_domain_into_the_8b027c_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_s2 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def partition_the_domain_into_the_8b027c_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def partition_the_domain_into_the_8b027c_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def partition_the_domain_into_the_8b027c_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def partition_the_domain_into_the_8b027c_s13 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def partition_the_domain_into_the_8b027c_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_l2 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def partition_the_domain_into_the_8b027c_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def partition_the_domain_into_the_8b027c_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def partition_the_domain_into_the_8b027c_l13 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def partition_the_domain_into_the_8b027c_protocol : IGProtocol partition_the_domain_into_the_8b027c_s0 partition_the_domain_into_the_8b027c_s13 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct partition_the_domain_into_the_8b027c_s9 partition_the_domain_into_the_8b027c_s9 = partition_the_domain_into_the_8b027c_s9 (idempotent)
  (.seq (.arrow partition_the_domain_into_the_8b027c_l0 partition_the_domain_into_the_8b027c_s0 partition_the_domain_into_the_8b027c_s1) (.seq (.prod (.arrow partition_the_domain_into_the_8b027c_l1 partition_the_domain_into_the_8b027c_s1 partition_the_domain_into_the_8b027c_s9) (.arrow partition_the_domain_into_the_8b027c_l1 partition_the_domain_into_the_8b027c_s1 partition_the_domain_into_the_8b027c_s9)) (.seq (.arrow partition_the_domain_into_the_8b027c_l9 partition_the_domain_into_the_8b027c_s9 partition_the_domain_into_the_8b027c_s9) (.seq (.arrow partition_the_domain_into_the_8b027c_l9 partition_the_domain_into_the_8b027c_s9 partition_the_domain_into_the_8b027c_s10) (.seq (.arrow partition_the_domain_into_the_8b027c_l10 partition_the_domain_into_the_8b027c_s10 partition_the_domain_into_the_8b027c_s11) (.seq (.arrow partition_the_domain_into_the_8b027c_l11 partition_the_domain_into_the_8b027c_s11 partition_the_domain_into_the_8b027c_s12) (.arrow partition_the_domain_into_the_8b027c_l12 partition_the_domain_into_the_8b027c_s12 partition_the_domain_into_the_8b027c_s13)))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def partition_the_domain_into_the_8b027c_true_arm : IGProtocol partition_the_domain_into_the_8b027c_s0 partition_the_domain_into_the_8b027c_s13 :=
  (partition_the_domain_into_the_8b027c_protocol).restrictToEVALT

-- false arm
noncomputable def partition_the_domain_into_the_8b027c_false_arm : IGProtocol partition_the_domain_into_the_8b027c_s0 partition_the_domain_into_the_8b027c_s13 :=
  (partition_the_domain_into_the_8b027c_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₂dag.
def partition_the_domain_into_the_8b027c_tier_ground : OuroboricityTier := TierFunctor.obj partition_the_domain_into_the_8b027c_s0
def partition_the_domain_into_the_8b027c_tier : OuroboricityTier := TierFunctor.obj partition_the_domain_into_the_8b027c_s13
#eval partition_the_domain_into_the_8b027c_tier_ground  -- tier of the ground (pre-transformation)
#eval partition_the_domain_into_the_8b027c_tier  -- the Grammar's own verdict on the closed object

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem partition_the_domain_into_the_8b027c_frobenius :
    igFrobeniusAlg.mul partition_the_domain_into_the_8b027c_s0 partition_the_domain_into_the_8b027c_s0 = partition_the_domain_into_the_8b027c_s0 :=
  igFrobAlg_self_fusion partition_the_domain_into_the_8b027c_s0
