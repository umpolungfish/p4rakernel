-- IGProtocol scaffold: ⊢ → ≻ → ∈ → ⊤ → ≺ → ⋈ → ⊥ → ⊞ → ∋ → ⊙ → ⊡ → ⊣
-- Class: Introduce the growth function g mapping natural numbers to natural numbers.
-- Fingerprint: sig=(6,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=12
-- Expected tier: O₂dag
-- ∈/∋ pairs: [(2, 8)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] ⊢     dim    := 𐑼               𐑼 → 𐑾  | initial object — ground of distinction
--   [1] ≻      rel    := 𐑾               𐑼 → 𐑚  | forward morphism — bidirectional arrow
--   [2] ∈    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [3] ⊤     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [4] ≺      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [5] ⋈     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [6] ⊥     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [7] ⊞    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [8] ∋     stoi   := 𐑙               𐑙 → 𐑠  | fuse μ — assembly mode
--   [9] ⊙   gram   := 𐑠               𐑙 → 𐑭  | identity — self-imscription
--   [10] ⊡      prot   := 𐑭               𐑠 → 𐑡  | irreversible fixation — winding number
--   [11] ⊣     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def introduce_the_growth_function_g_mapping_2ba4bd_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_s1 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_s2 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := up, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def introduce_the_growth_function_g_mapping_2ba4bd_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def introduce_the_growth_function_g_mapping_2ba4bd_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_l1 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def introduce_the_growth_function_g_mapping_2ba4bd_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def introduce_the_growth_function_g_mapping_2ba4bd_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def introduce_the_growth_function_g_mapping_2ba4bd_protocol : IGProtocol introduce_the_growth_function_g_mapping_2ba4bd_s0 introduce_the_growth_function_g_mapping_2ba4bd_s11 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct introduce_the_growth_function_g_mapping_2ba4bd_s8 introduce_the_growth_function_g_mapping_2ba4bd_s8 = introduce_the_growth_function_g_mapping_2ba4bd_s8 (idempotent)
  (.seq (.arrow introduce_the_growth_function_g_mapping_2ba4bd_l0 introduce_the_growth_function_g_mapping_2ba4bd_s0 introduce_the_growth_function_g_mapping_2ba4bd_s1) (.seq (.arrow introduce_the_growth_function_g_mapping_2ba4bd_l1 introduce_the_growth_function_g_mapping_2ba4bd_s1 introduce_the_growth_function_g_mapping_2ba4bd_s2) (.seq (.prod (.arrow introduce_the_growth_function_g_mapping_2ba4bd_l2 introduce_the_growth_function_g_mapping_2ba4bd_s2 introduce_the_growth_function_g_mapping_2ba4bd_s8) (.arrow introduce_the_growth_function_g_mapping_2ba4bd_l2 introduce_the_growth_function_g_mapping_2ba4bd_s2 introduce_the_growth_function_g_mapping_2ba4bd_s8)) (.seq (.arrow introduce_the_growth_function_g_mapping_2ba4bd_l8 introduce_the_growth_function_g_mapping_2ba4bd_s8 introduce_the_growth_function_g_mapping_2ba4bd_s8) (.seq (.arrow introduce_the_growth_function_g_mapping_2ba4bd_l8 introduce_the_growth_function_g_mapping_2ba4bd_s8 introduce_the_growth_function_g_mapping_2ba4bd_s9) (.seq (.arrow introduce_the_growth_function_g_mapping_2ba4bd_l9 introduce_the_growth_function_g_mapping_2ba4bd_s9 introduce_the_growth_function_g_mapping_2ba4bd_s10) (.arrow introduce_the_growth_function_g_mapping_2ba4bd_l10 introduce_the_growth_function_g_mapping_2ba4bd_s10 introduce_the_growth_function_g_mapping_2ba4bd_s11)))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def introduce_the_growth_function_g_mapping_2ba4bd_true_arm : IGProtocol introduce_the_growth_function_g_mapping_2ba4bd_s0 introduce_the_growth_function_g_mapping_2ba4bd_s11 :=
  (introduce_the_growth_function_g_mapping_2ba4bd_protocol).restrictToEVALT

-- false arm
noncomputable def introduce_the_growth_function_g_mapping_2ba4bd_false_arm : IGProtocol introduce_the_growth_function_g_mapping_2ba4bd_s0 introduce_the_growth_function_g_mapping_2ba4bd_s11 :=
  (introduce_the_growth_function_g_mapping_2ba4bd_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₂dag.
def introduce_the_growth_function_g_mapping_2ba4bd_tier_ground : OuroboricityTier := TierFunctor.obj introduce_the_growth_function_g_mapping_2ba4bd_s0
def introduce_the_growth_function_g_mapping_2ba4bd_tier : OuroboricityTier := TierFunctor.obj introduce_the_growth_function_g_mapping_2ba4bd_s11
#eval introduce_the_growth_function_g_mapping_2ba4bd_tier_ground  -- tier of the ground (pre-transformation)
#eval introduce_the_growth_function_g_mapping_2ba4bd_tier  -- the Grammar's own verdict on the closed object

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem introduce_the_growth_function_g_mapping_2ba4bd_frobenius :
    igFrobeniusAlg.mul introduce_the_growth_function_g_mapping_2ba4bd_s0 introduce_the_growth_function_g_mapping_2ba4bd_s0 = introduce_the_growth_function_g_mapping_2ba4bd_s0 :=
  igFrobAlg_self_fusion introduce_the_growth_function_g_mapping_2ba4bd_s0
