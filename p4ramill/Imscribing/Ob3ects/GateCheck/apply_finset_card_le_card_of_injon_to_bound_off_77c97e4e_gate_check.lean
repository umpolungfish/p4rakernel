-- IGProtocol scaffold: VINIT → CLINK → FSPLIT → EVALT → EVALF → CLINK → FFUSE → CLINK → CLINK → IFIX → TANCH
-- Class: Apply Finset.card_le_card_of_injOn to bound off-diagonal cardinality
-- Fingerprint: sig=(6,2,2,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=False | period=11
-- Expected tier: O₂dag
-- FSPLIT/FFUSE pairs: [(2, 6)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑱  | initial object — ground of distinction
--   [1] CLINK     fid    := 𐑱               𐑼 → 𐑚  | composition — regime coherence
--   [2] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [3] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [4] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [5] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [6] FFUSE     stoi   := 𐑙               𐑙 → 𐑱  | fuse μ — assembly mode
--   [7] CLINK     fid    := 𐑱               𐑙 → 𐑱  | composition — regime coherence
--   [8] CLINK     fid    := 𐑱               𐑱 → 𐑭  | composition — regime coherence
--   [9] IFIX      prot   := 𐑭               𐑱 → 𐑡  | irreversible fixation — winding number
--   [10] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def apply_finset_card_le_card_of_injon_to_454af2_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_s3 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_s4 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_s5 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_s6 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_s7 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_s8 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_s9 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := ah }
private def apply_finset_card_le_card_of_injon_to_454af2_s10 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def apply_finset_card_le_card_of_injon_to_454af2_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def apply_finset_card_le_card_of_injon_to_454af2_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def apply_finset_card_le_card_of_injon_to_454af2_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def apply_finset_card_le_card_of_injon_to_454af2_protocol : IGProtocol apply_finset_card_le_card_of_injon_to_454af2_s0 apply_finset_card_le_card_of_injon_to_454af2_s10 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct apply_finset_card_le_card_of_injon_to_454af2_s6 apply_finset_card_le_card_of_injon_to_454af2_s6 = apply_finset_card_le_card_of_injon_to_454af2_s6 (idempotent)
  (.seq (.arrow apply_finset_card_le_card_of_injon_to_454af2_l0 apply_finset_card_le_card_of_injon_to_454af2_s0 apply_finset_card_le_card_of_injon_to_454af2_s1) (.seq (.arrow apply_finset_card_le_card_of_injon_to_454af2_l1 apply_finset_card_le_card_of_injon_to_454af2_s1 apply_finset_card_le_card_of_injon_to_454af2_s2) (.seq (.prod (.arrow apply_finset_card_le_card_of_injon_to_454af2_l2 apply_finset_card_le_card_of_injon_to_454af2_s2 apply_finset_card_le_card_of_injon_to_454af2_s6) (.arrow apply_finset_card_le_card_of_injon_to_454af2_l2 apply_finset_card_le_card_of_injon_to_454af2_s2 apply_finset_card_le_card_of_injon_to_454af2_s6)) (.seq (.arrow apply_finset_card_le_card_of_injon_to_454af2_l6 apply_finset_card_le_card_of_injon_to_454af2_s6 apply_finset_card_le_card_of_injon_to_454af2_s6) (.seq (.arrow apply_finset_card_le_card_of_injon_to_454af2_l6 apply_finset_card_le_card_of_injon_to_454af2_s6 apply_finset_card_le_card_of_injon_to_454af2_s7) (.seq (.arrow apply_finset_card_le_card_of_injon_to_454af2_l7 apply_finset_card_le_card_of_injon_to_454af2_s7 apply_finset_card_le_card_of_injon_to_454af2_s8) (.seq (.arrow apply_finset_card_le_card_of_injon_to_454af2_l8 apply_finset_card_le_card_of_injon_to_454af2_s8 apply_finset_card_le_card_of_injon_to_454af2_s9) (.arrow apply_finset_card_le_card_of_injon_to_454af2_l9 apply_finset_card_le_card_of_injon_to_454af2_s9 apply_finset_card_le_card_of_injon_to_454af2_s10))))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def apply_finset_card_le_card_of_injon_to_454af2_true_arm : IGProtocol apply_finset_card_le_card_of_injon_to_454af2_s0 apply_finset_card_le_card_of_injon_to_454af2_s10 :=
  (apply_finset_card_le_card_of_injon_to_454af2_protocol).restrictToEVALT

-- false arm
noncomputable def apply_finset_card_le_card_of_injon_to_454af2_false_arm : IGProtocol apply_finset_card_le_card_of_injon_to_454af2_s0 apply_finset_card_le_card_of_injon_to_454af2_s10 :=
  (apply_finset_card_le_card_of_injon_to_454af2_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₂dag.
def apply_finset_card_le_card_of_injon_to_454af2_tier_ground : OuroboricityTier := TierFunctor.obj apply_finset_card_le_card_of_injon_to_454af2_s0
def apply_finset_card_le_card_of_injon_to_454af2_tier : OuroboricityTier := TierFunctor.obj apply_finset_card_le_card_of_injon_to_454af2_s10
#eval apply_finset_card_le_card_of_injon_to_454af2_tier_ground  -- tier of the ground (pre-transformation)
#eval apply_finset_card_le_card_of_injon_to_454af2_tier  -- the Grammar's own verdict on the closed object

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem apply_finset_card_le_card_of_injon_to_454af2_frobenius :
    igFrobeniusAlg.mul apply_finset_card_le_card_of_injon_to_454af2_s0 apply_finset_card_le_card_of_injon_to_454af2_s0 = apply_finset_card_le_card_of_injon_to_454af2_s0 :=
  igFrobAlg_self_fusion apply_finset_card_le_card_of_injon_to_454af2_s0
