-- IGProtocol scaffold: ⊢ → ⊣ → ∈ → ≻ → ⋈ → ⊤ → ≺ → ⊥ → ⊞ → ∋ → ⊙ → ⊡ → ⊣
-- Class: Rejoin the constructive attempt and the gap into a single open problem state
-- Fingerprint: sig=(7,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=13
-- Expected tier: O₂dag
-- ∈/∋ pairs: [(2, 9)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] ⊢     dim    := 𐑼               𐑼 → 𐑡  | initial object — ground of distinction
--   [1] ⊣     top    := 𐑡               𐑼 → 𐑚  | terminal object — connectivity boundary
--   [2] ∈    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [3] ≻      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [4] ⋈     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [5] ⊤     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [6] ≺      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [7] ⊥     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [8] ⊞    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [9] ∋     stoi   := 𐑙               𐑙 → 𐑠  | fuse μ — assembly mode
--   [10] ⊙   gram   := 𐑠               𐑙 → 𐑭  | identity — self-imscription
--   [11] ⊡      prot   := 𐑭               𐑠 → 𐑡  | irreversible fixation — winding number
--   [12] ⊣     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def rejoin_the_constructive_attempt_and_the_ff0ccd_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := up, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def rejoin_the_constructive_attempt_and_the_ff0ccd_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_l3 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def rejoin_the_constructive_attempt_and_the_ff0ccd_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def rejoin_the_constructive_attempt_and_the_ff0ccd_protocol : IGProtocol rejoin_the_constructive_attempt_and_the_ff0ccd_s0 rejoin_the_constructive_attempt_and_the_ff0ccd_s12 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct rejoin_the_constructive_attempt_and_the_ff0ccd_s9 rejoin_the_constructive_attempt_and_the_ff0ccd_s9 = rejoin_the_constructive_attempt_and_the_ff0ccd_s9 (idempotent)
  (.seq (.arrow rejoin_the_constructive_attempt_and_the_ff0ccd_l0 rejoin_the_constructive_attempt_and_the_ff0ccd_s0 rejoin_the_constructive_attempt_and_the_ff0ccd_s1) (.seq (.arrow rejoin_the_constructive_attempt_and_the_ff0ccd_l1 rejoin_the_constructive_attempt_and_the_ff0ccd_s1 rejoin_the_constructive_attempt_and_the_ff0ccd_s2) (.seq (.prod (.arrow rejoin_the_constructive_attempt_and_the_ff0ccd_l2 rejoin_the_constructive_attempt_and_the_ff0ccd_s2 rejoin_the_constructive_attempt_and_the_ff0ccd_s9) (.arrow rejoin_the_constructive_attempt_and_the_ff0ccd_l2 rejoin_the_constructive_attempt_and_the_ff0ccd_s2 rejoin_the_constructive_attempt_and_the_ff0ccd_s9)) (.seq (.arrow rejoin_the_constructive_attempt_and_the_ff0ccd_l9 rejoin_the_constructive_attempt_and_the_ff0ccd_s9 rejoin_the_constructive_attempt_and_the_ff0ccd_s9) (.seq (.arrow rejoin_the_constructive_attempt_and_the_ff0ccd_l9 rejoin_the_constructive_attempt_and_the_ff0ccd_s9 rejoin_the_constructive_attempt_and_the_ff0ccd_s10) (.seq (.arrow rejoin_the_constructive_attempt_and_the_ff0ccd_l10 rejoin_the_constructive_attempt_and_the_ff0ccd_s10 rejoin_the_constructive_attempt_and_the_ff0ccd_s11) (.arrow rejoin_the_constructive_attempt_and_the_ff0ccd_l11 rejoin_the_constructive_attempt_and_the_ff0ccd_s11 rejoin_the_constructive_attempt_and_the_ff0ccd_s12)))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def rejoin_the_constructive_attempt_and_the_ff0ccd_true_arm : IGProtocol rejoin_the_constructive_attempt_and_the_ff0ccd_s0 rejoin_the_constructive_attempt_and_the_ff0ccd_s12 :=
  (rejoin_the_constructive_attempt_and_the_ff0ccd_protocol).restrictToEVALT

-- false arm
noncomputable def rejoin_the_constructive_attempt_and_the_ff0ccd_false_arm : IGProtocol rejoin_the_constructive_attempt_and_the_ff0ccd_s0 rejoin_the_constructive_attempt_and_the_ff0ccd_s12 :=
  (rejoin_the_constructive_attempt_and_the_ff0ccd_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₂dag.
def rejoin_the_constructive_attempt_and_the_ff0ccd_tier_ground : OuroboricityTier := TierFunctor.obj rejoin_the_constructive_attempt_and_the_ff0ccd_s0
def rejoin_the_constructive_attempt_and_the_ff0ccd_tier : OuroboricityTier := TierFunctor.obj rejoin_the_constructive_attempt_and_the_ff0ccd_s12
#eval rejoin_the_constructive_attempt_and_the_ff0ccd_tier_ground  -- tier of the ground (pre-transformation)
#eval rejoin_the_constructive_attempt_and_the_ff0ccd_tier  -- the Grammar's own verdict on the closed object

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem rejoin_the_constructive_attempt_and_the_ff0ccd_frobenius :
    igFrobeniusAlg.mul rejoin_the_constructive_attempt_and_the_ff0ccd_s0 rejoin_the_constructive_attempt_and_the_ff0ccd_s0 = rejoin_the_constructive_attempt_and_the_ff0ccd_s0 :=
  igFrobAlg_self_fusion rejoin_the_constructive_attempt_and_the_ff0ccd_s0
