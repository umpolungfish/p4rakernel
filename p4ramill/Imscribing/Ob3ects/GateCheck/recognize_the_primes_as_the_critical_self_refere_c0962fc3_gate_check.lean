-- IGProtocol scaffold: VINIT → IMSCRIB → FSPLIT → AFWD → EVALT → AREV → EVALF → ENGAGR → CLINK → FFUSE → IFIX → TANCH
-- Class: Recognize the primes as the critical self-referential element that satisfies divergence but fails density.
-- Fingerprint: sig=(6,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=12
-- Expected tier: O₂dag
-- FSPLIT/FFUSE pairs: [(2, 9)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑠  | initial object — ground of distinction
--   [1] IMSCRIB   gram   := 𐑠               𐑼 → 𐑚  | identity — self-imscription
--   [2] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [3] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [4] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [5] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [6] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [7] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [8] CLINK     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [9] FFUSE     stoi   := 𐑙               𐑙 → 𐑭  | fuse μ — assembly mode
--   [10] IFIX      prot   := 𐑭               𐑙 → 𐑡  | irreversible fixation — winding number
--   [11] TANCH     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def recognize_the_primes_as_the_critical_edb626_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := fee, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def recognize_the_primes_as_the_critical_edb626_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def recognize_the_primes_as_the_critical_edb626_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_l3 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def recognize_the_primes_as_the_critical_edb626_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def recognize_the_primes_as_the_critical_edb626_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def recognize_the_primes_as_the_critical_edb626_protocol : IGProtocol recognize_the_primes_as_the_critical_edb626_s0 recognize_the_primes_as_the_critical_edb626_s11 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct recognize_the_primes_as_the_critical_edb626_s9 recognize_the_primes_as_the_critical_edb626_s9 = recognize_the_primes_as_the_critical_edb626_s9 (idempotent)
  (.seq (.arrow recognize_the_primes_as_the_critical_edb626_l0 recognize_the_primes_as_the_critical_edb626_s0 recognize_the_primes_as_the_critical_edb626_s1) (.seq (.arrow recognize_the_primes_as_the_critical_edb626_l1 recognize_the_primes_as_the_critical_edb626_s1 recognize_the_primes_as_the_critical_edb626_s2) (.seq (.prod (.arrow recognize_the_primes_as_the_critical_edb626_l2 recognize_the_primes_as_the_critical_edb626_s2 recognize_the_primes_as_the_critical_edb626_s9) (.arrow recognize_the_primes_as_the_critical_edb626_l2 recognize_the_primes_as_the_critical_edb626_s2 recognize_the_primes_as_the_critical_edb626_s9)) (.seq (.arrow recognize_the_primes_as_the_critical_edb626_l9 recognize_the_primes_as_the_critical_edb626_s9 recognize_the_primes_as_the_critical_edb626_s9) (.seq (.arrow recognize_the_primes_as_the_critical_edb626_l9 recognize_the_primes_as_the_critical_edb626_s9 recognize_the_primes_as_the_critical_edb626_s10) (.arrow recognize_the_primes_as_the_critical_edb626_l10 recognize_the_primes_as_the_critical_edb626_s10 recognize_the_primes_as_the_critical_edb626_s11))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def recognize_the_primes_as_the_critical_edb626_true_arm : IGProtocol recognize_the_primes_as_the_critical_edb626_s0 recognize_the_primes_as_the_critical_edb626_s11 :=
  (recognize_the_primes_as_the_critical_edb626_protocol).restrictToEVALT

-- false arm
noncomputable def recognize_the_primes_as_the_critical_edb626_false_arm : IGProtocol recognize_the_primes_as_the_critical_edb626_s0 recognize_the_primes_as_the_critical_edb626_s11 :=
  (recognize_the_primes_as_the_critical_edb626_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₂dag.
def recognize_the_primes_as_the_critical_edb626_tier_ground : OuroboricityTier := TierFunctor.obj recognize_the_primes_as_the_critical_edb626_s0
def recognize_the_primes_as_the_critical_edb626_tier : OuroboricityTier := TierFunctor.obj recognize_the_primes_as_the_critical_edb626_s11
#eval recognize_the_primes_as_the_critical_edb626_tier_ground  -- tier of the ground (pre-transformation)
#eval recognize_the_primes_as_the_critical_edb626_tier  -- the Grammar's own verdict on the closed object

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem recognize_the_primes_as_the_critical_edb626_frobenius :
    igFrobeniusAlg.mul recognize_the_primes_as_the_critical_edb626_s0 recognize_the_primes_as_the_critical_edb626_s0 = recognize_the_primes_as_the_critical_edb626_s0 :=
  igFrobAlg_self_fusion recognize_the_primes_as_the_critical_edb626_s0
