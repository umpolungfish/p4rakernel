-- IGProtocol scaffold: ⊢ → ⊣ → ∈ → ≻ → ⋈ → ⊤ → ≺ → ⊥ → ⊞ → ∋ → ⊙ → ⊡ → ⊣
-- Class: Branch the domain into the constructive proof arm and the unresolved gap arm
-- Fingerprint: sig=(7,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=13
-- Expected tier: O₂dag
-- ∈/∋ pairs: [(2, 9)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] ⊢     dim    := 𐑼               𐑼 → 𐑡  | initial object — ground of distinction
--   [1] ⊣     top    := 𐑡               𐑼 → 𐑚  | terminal object — connectivity boundary
--   [2] ∈    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [3] ≻      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [4] ⋈     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [5] ⊤     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [6] ≺      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [7] ⊥     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [8] ⊞    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [9] ∋     stoi   := 𐑙               𐑙 → 𐑠  | fuse μ — assembly mode
--   [10] ⊙   gram   := 𐑠               𐑙 → 𐑭  | identity — self-imscription
--   [11] ⊡      prot   := 𐑭               𐑠 → 𐑡  | irreversible fixation — winding number
--   [12] ⊣     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def branch_the_domain_into_the_constructive_77ece6_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := up, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def branch_the_domain_into_the_constructive_77ece6_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def branch_the_domain_into_the_constructive_77ece6_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_l3 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def branch_the_domain_into_the_constructive_77ece6_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def branch_the_domain_into_the_constructive_77ece6_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def branch_the_domain_into_the_constructive_77ece6_protocol : IGProtocol branch_the_domain_into_the_constructive_77ece6_s0 branch_the_domain_into_the_constructive_77ece6_s12 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct branch_the_domain_into_the_constructive_77ece6_s9 branch_the_domain_into_the_constructive_77ece6_s9 = branch_the_domain_into_the_constructive_77ece6_s9 (idempotent)
  (.seq (.arrow branch_the_domain_into_the_constructive_77ece6_l0 branch_the_domain_into_the_constructive_77ece6_s0 branch_the_domain_into_the_constructive_77ece6_s1) (.seq (.arrow branch_the_domain_into_the_constructive_77ece6_l1 branch_the_domain_into_the_constructive_77ece6_s1 branch_the_domain_into_the_constructive_77ece6_s2) (.seq (.prod (.arrow branch_the_domain_into_the_constructive_77ece6_l2 branch_the_domain_into_the_constructive_77ece6_s2 branch_the_domain_into_the_constructive_77ece6_s9) (.arrow branch_the_domain_into_the_constructive_77ece6_l2 branch_the_domain_into_the_constructive_77ece6_s2 branch_the_domain_into_the_constructive_77ece6_s9)) (.seq (.arrow branch_the_domain_into_the_constructive_77ece6_l9 branch_the_domain_into_the_constructive_77ece6_s9 branch_the_domain_into_the_constructive_77ece6_s9) (.seq (.arrow branch_the_domain_into_the_constructive_77ece6_l9 branch_the_domain_into_the_constructive_77ece6_s9 branch_the_domain_into_the_constructive_77ece6_s10) (.seq (.arrow branch_the_domain_into_the_constructive_77ece6_l10 branch_the_domain_into_the_constructive_77ece6_s10 branch_the_domain_into_the_constructive_77ece6_s11) (.arrow branch_the_domain_into_the_constructive_77ece6_l11 branch_the_domain_into_the_constructive_77ece6_s11 branch_the_domain_into_the_constructive_77ece6_s12)))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def branch_the_domain_into_the_constructive_77ece6_true_arm : IGProtocol branch_the_domain_into_the_constructive_77ece6_s0 branch_the_domain_into_the_constructive_77ece6_s12 :=
  (branch_the_domain_into_the_constructive_77ece6_protocol).restrictToEVALT

-- false arm
noncomputable def branch_the_domain_into_the_constructive_77ece6_false_arm : IGProtocol branch_the_domain_into_the_constructive_77ece6_s0 branch_the_domain_into_the_constructive_77ece6_s12 :=
  (branch_the_domain_into_the_constructive_77ece6_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₂dag.
def branch_the_domain_into_the_constructive_77ece6_tier_ground : OuroboricityTier := TierFunctor.obj branch_the_domain_into_the_constructive_77ece6_s0
def branch_the_domain_into_the_constructive_77ece6_tier : OuroboricityTier := TierFunctor.obj branch_the_domain_into_the_constructive_77ece6_s12
#eval branch_the_domain_into_the_constructive_77ece6_tier_ground  -- tier of the ground (pre-transformation)
#eval branch_the_domain_into_the_constructive_77ece6_tier  -- the Grammar's own verdict on the closed object

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem branch_the_domain_into_the_constructive_77ece6_frobenius :
    igFrobeniusAlg.mul branch_the_domain_into_the_constructive_77ece6_s0 branch_the_domain_into_the_constructive_77ece6_s0 = branch_the_domain_into_the_constructive_77ece6_s0 :=
  igFrobAlg_self_fusion branch_the_domain_into_the_constructive_77ece6_s0
