-- IGProtocol scaffold: ⊢ → ⊣ → ∈ → ≻ → ⋈ → ⊤ → ≺ → ⊥ → ⊞ → ∋ → ⊙ → ⊡ → ⊣
-- Class: Generate subsets S and T from A in the constructive arm
-- Fingerprint: sig=(7,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=13
-- Expected tier: O₂dag
-- ∈/∋ pairs: [(2, 9)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] ⊢     dim    := 𐑼               𐑼 → 𐑡  | initial object — ground of distinction
--   [1] ⊣     top    := 𐑡               𐑼 → 𐑚  | terminal object — connectivity boundary
--   [2] ∈    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [3] ≻      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [4] ⋈     fid    := 𐑱               𐑚 → 𐑙  | composition — regime coherence
--   [5] ⊤     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [6] ≺      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [7] ⊥     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [8] ⊞    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [9] ∋     stoi   := 𐑙               𐑙 → 𐑠  | fuse μ — assembly mode
--   [10] ⊙   gram   := 𐑠               𐑙 → 𐑭  | identity — self-imscription
--   [11] ⊡      prot   := 𐑭               𐑠 → 𐑡  | irreversible fixation — winding number
--   [12] ⊣     top    := 𐑡               𐑭 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_s3 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_s4 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_s5 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_s6 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_s7 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_s8 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := up, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_s9 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_s10 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_s11 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_s12 : Imscription :=
  { dim := array, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_l3 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_l10 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_l11 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def generate_subsets_s_and_t_from_a_in_the_7b9f11_l12 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def generate_subsets_s_and_t_from_a_in_the_7b9f11_protocol : IGProtocol generate_subsets_s_and_t_from_a_in_the_7b9f11_s0 generate_subsets_s_and_t_from_a_in_the_7b9f11_s12 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct generate_subsets_s_and_t_from_a_in_the_7b9f11_s9 generate_subsets_s_and_t_from_a_in_the_7b9f11_s9 = generate_subsets_s_and_t_from_a_in_the_7b9f11_s9 (idempotent)
  (.seq (.arrow generate_subsets_s_and_t_from_a_in_the_7b9f11_l0 generate_subsets_s_and_t_from_a_in_the_7b9f11_s0 generate_subsets_s_and_t_from_a_in_the_7b9f11_s1) (.seq (.arrow generate_subsets_s_and_t_from_a_in_the_7b9f11_l1 generate_subsets_s_and_t_from_a_in_the_7b9f11_s1 generate_subsets_s_and_t_from_a_in_the_7b9f11_s2) (.seq (.prod (.arrow generate_subsets_s_and_t_from_a_in_the_7b9f11_l2 generate_subsets_s_and_t_from_a_in_the_7b9f11_s2 generate_subsets_s_and_t_from_a_in_the_7b9f11_s9) (.arrow generate_subsets_s_and_t_from_a_in_the_7b9f11_l2 generate_subsets_s_and_t_from_a_in_the_7b9f11_s2 generate_subsets_s_and_t_from_a_in_the_7b9f11_s9)) (.seq (.arrow generate_subsets_s_and_t_from_a_in_the_7b9f11_l9 generate_subsets_s_and_t_from_a_in_the_7b9f11_s9 generate_subsets_s_and_t_from_a_in_the_7b9f11_s9) (.seq (.arrow generate_subsets_s_and_t_from_a_in_the_7b9f11_l9 generate_subsets_s_and_t_from_a_in_the_7b9f11_s9 generate_subsets_s_and_t_from_a_in_the_7b9f11_s10) (.seq (.arrow generate_subsets_s_and_t_from_a_in_the_7b9f11_l10 generate_subsets_s_and_t_from_a_in_the_7b9f11_s10 generate_subsets_s_and_t_from_a_in_the_7b9f11_s11) (.arrow generate_subsets_s_and_t_from_a_in_the_7b9f11_l11 generate_subsets_s_and_t_from_a_in_the_7b9f11_s11 generate_subsets_s_and_t_from_a_in_the_7b9f11_s12)))))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- truth arm
noncomputable def generate_subsets_s_and_t_from_a_in_the_7b9f11_true_arm : IGProtocol generate_subsets_s_and_t_from_a_in_the_7b9f11_s0 generate_subsets_s_and_t_from_a_in_the_7b9f11_s12 :=
  (generate_subsets_s_and_t_from_a_in_the_7b9f11_protocol).restrictToEVALT

-- false arm
noncomputable def generate_subsets_s_and_t_from_a_in_the_7b9f11_false_arm : IGProtocol generate_subsets_s_and_t_from_a_in_the_7b9f11_s0 generate_subsets_s_and_t_from_a_in_the_7b9f11_s12 :=
  (generate_subsets_s_and_t_from_a_in_the_7b9f11_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₂dag.
def generate_subsets_s_and_t_from_a_in_the_7b9f11_tier_ground : OuroboricityTier := TierFunctor.obj generate_subsets_s_and_t_from_a_in_the_7b9f11_s0
def generate_subsets_s_and_t_from_a_in_the_7b9f11_tier : OuroboricityTier := TierFunctor.obj generate_subsets_s_and_t_from_a_in_the_7b9f11_s12
#eval generate_subsets_s_and_t_from_a_in_the_7b9f11_tier_ground  -- tier of the ground (pre-transformation)
#eval generate_subsets_s_and_t_from_a_in_the_7b9f11_tier  -- the Grammar's own verdict on the closed object

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem generate_subsets_s_and_t_from_a_in_the_7b9f11_frobenius :
    igFrobeniusAlg.mul generate_subsets_s_and_t_from_a_in_the_7b9f11_s0 generate_subsets_s_and_t_from_a_in_the_7b9f11_s0 = generate_subsets_s_and_t_from_a_in_the_7b9f11_s0 :=
  igFrobAlg_self_fusion generate_subsets_s_and_t_from_a_in_the_7b9f11_s0
