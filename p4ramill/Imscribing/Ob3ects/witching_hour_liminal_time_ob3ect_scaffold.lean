-- IGProtocol scaffold: IMSCRIB → AREV → FSPLIT → AFWD → FFUSE → CLINK → IFIX → IMSCRIB
-- Class: Witching hour liminal time-ob3ect: the threshold moment (3 AM, Samhain, solstices) when the veil between worlds thins. FSPLIT: the liminal moment splits time into the ordinary flow and the timeless interval where past/present/future coexist. FFUSE: the moment passes, reconstituting ordinary time — but with something carried across the threshold. VINIT: time before the thinning. TANCH: the clock striking the hour. AFWD: the veil opens. AREV: the veil closes. ENGAGR: the moment is simultaneously within time (it has a clock position) and outside time (it touches eternity).
-- Fingerprint: sig=(5,2,0,1)
--   self_ref=True | frobenius_order=1
--   dialetheia_complete=False | period=8
-- Expected tier: O₀
-- FSPLIT/FFUSE pairs: [(2, 4)]

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality
     --
-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] IMSCRIB   gram   := 𐑠               𐑠 → 𐑗  | identity — self-imscription
--   [1] AREV      pol    := 𐑗               𐑠 → 𐑚  | reverse morphism — parity flip
--   [2] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [3] AFWD      rel    := 𐑾               𐑚 → 𐑙  | forward morphism — bidirectional arrow
--   [4] FFUSE     stoi   := 𐑙               𐑙 → 𐑱  | fuse μ — assembly mode
--   [5] CLINK     fid    := 𐑱               𐑙 → 𐑭  | composition — regime coherence
--   [6] IFIX      prot   := 𐑭               𐑱 → 𐑠  | irreversible fixation — winding number
--   [7] IMSCRIB   gram   := 𐑠               𐑭 → 𐑠  | identity — self-imscription

-- ── Back-propagation edges (self-referential loop) ──────────────────────
--   IMSCRIB positions: [0, 7]
--   IFIX    positions: [6]
--   Back-prop: IMSCRIB→IFIX (LinFix) — igProtoCopy_isDagger axiom applies
--   Weighted: CLINK→IMSCRIB — feeds next winding via .seq after .prod

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def witching_hour_liminal_time_ob3ect_the_1b4142_s0 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def witching_hour_liminal_time_ob3ect_the_1b4142_s1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def witching_hour_liminal_time_ob3ect_the_1b4142_s2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def witching_hour_liminal_time_ob3ect_the_1b4142_s3 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def witching_hour_liminal_time_ob3ect_the_1b4142_s4 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def witching_hour_liminal_time_ob3ect_the_1b4142_s5 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def witching_hour_liminal_time_ob3ect_the_1b4142_s6 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
private def witching_hour_liminal_time_ob3ect_the_1b4142_s7 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := woe, chir := fee, stoi := hung, prot := ah }
  --
-- ── Label Imscriptions (per-node delta) ─────────────────────
private def witching_hour_liminal_time_ob3ect_the_1b4142_l0 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def witching_hour_liminal_time_ob3ect_the_1b4142_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def witching_hour_liminal_time_ob3ect_the_1b4142_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def witching_hour_liminal_time_ob3ect_the_1b4142_l3 : Imscription :=
  { dim := dead, top := judge, rel := ian, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def witching_hour_liminal_time_ob3ect_the_1b4142_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def witching_hour_liminal_time_ob3ect_the_1b4142_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def witching_hour_liminal_time_ob3ect_the_1b4142_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def witching_hour_liminal_time_ob3ect_the_1b4142_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
  --
-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def witching_hour_liminal_time_ob3ect_the_1b4142_protocol : IGProtocol witching_hour_liminal_time_ob3ect_the_1b4142_s0 witching_hour_liminal_time_ob3ect_the_1b4142_s7 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct witching_hour_liminal_time_ob3ect_the_1b4142_s4 witching_hour_liminal_time_ob3ect_the_1b4142_s4 = witching_hour_liminal_time_ob3ect_the_1b4142_s4 (idempotent)
  (.seq (.arrow witching_hour_liminal_time_ob3ect_the_1b4142_l0 witching_hour_liminal_time_ob3ect_the_1b4142_s0 witching_hour_liminal_time_ob3ect_the_1b4142_s1) (.seq (.arrow witching_hour_liminal_time_ob3ect_the_1b4142_l1 witching_hour_liminal_time_ob3ect_the_1b4142_s1 witching_hour_liminal_time_ob3ect_the_1b4142_s2) (.seq (.prod (.arrow witching_hour_liminal_time_ob3ect_the_1b4142_l2 witching_hour_liminal_time_ob3ect_the_1b4142_s2 witching_hour_liminal_time_ob3ect_the_1b4142_s4) (.arrow witching_hour_liminal_time_ob3ect_the_1b4142_l2 witching_hour_liminal_time_ob3ect_the_1b4142_s2 witching_hour_liminal_time_ob3ect_the_1b4142_s4)) (.seq (.arrow witching_hour_liminal_time_ob3ect_the_1b4142_l4 witching_hour_liminal_time_ob3ect_the_1b4142_s4 witching_hour_liminal_time_ob3ect_the_1b4142_s4) (.seq (.arrow witching_hour_liminal_time_ob3ect_the_1b4142_l4 witching_hour_liminal_time_ob3ect_the_1b4142_s4 witching_hour_liminal_time_ob3ect_the_1b4142_s5) (.seq (.arrow witching_hour_liminal_time_ob3ect_the_1b4142_l5 witching_hour_liminal_time_ob3ect_the_1b4142_s5 witching_hour_liminal_time_ob3ect_the_1b4142_s6) (.arrow witching_hour_liminal_time_ob3ect_the_1b4142_l6 witching_hour_liminal_time_ob3ect_the_1b4142_s6 witching_hour_liminal_time_ob3ect_the_1b4142_s7)))))))
  --
-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₀.
def witching_hour_liminal_time_ob3ect_the_1b4142_tier : OuroboricityTier := TierFunctor.obj witching_hour_liminal_time_ob3ect_the_1b4142_s0
#eval witching_hour_liminal_time_ob3ect_the_1b4142_tier  -- the Grammar's own verdict on its tier

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem witching_hour_liminal_time_ob3ect_the_1b4142_frobenius :
    igFrobeniusAlg.mul witching_hour_liminal_time_ob3ect_the_1b4142_s0 witching_hour_liminal_time_ob3ect_the_1b4142_s0 = witching_hour_liminal_time_ob3ect_the_1b4142_s0 :=
  igFrobAlg_self_fusion witching_hour_liminal_time_ob3ect_the_1b4142_s0
  --
-- Self-reference: Δ is a dagger and μ = Δ†
theorem witching_hour_liminal_time_ob3ect_the_1b4142_self_ref :
    (igProtoDelta witching_hour_liminal_time_ob3ect_the_1b4142_s0 (by decide)).isDagger = true ∧
    igProtoMu_depth (paralogical_dagger (by decide)) = 1 := by
  constructor
  · exact igProtoCopy_isDagger
  · exact igProtoMu_depth
  --
-- Loop closure: period=8, depth=1
theorem witching_hour_liminal_time_ob3ect_the_1b4142_loop_closure :
    ∃ (loop : IGProtocol witching_hour_liminal_time_ob3ect_the_1b4142_s0 witching_hour_liminal_time_ob3ect_the_1b4142_s7),
      loop = witching_hour_liminal_time_ob3ect_the_1b4142_protocol ∧
      loop.period = 8 ∧ loop.depth = 1 := by
  exact ⟨_, rfl, by decide, by decide⟩
  --
-- igProtoCopy_isDagger licenses IMSCRIB→IFIX burn
-- CLINK→IMSCRIB weighted edge: .seq continuation
