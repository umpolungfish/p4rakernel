-- IGProtocol scaffold: VINIT → FSPLIT → EVALT → EVALF → AREV → IMSCRIB → ENGAGR → IFIX → FFUSE → TANCH
-- Class: falsifiability_degenerate
-- Fingerprint: sig=(4,2,3,1)
--   self_ref=False | frobenius_order=1
--   dialetheia_complete=True | period=10
-- Expected tier: O₂dag
-- FSPLIT/FFUSE pairs: [(1, 8)]
--
-- Generated by proof_scaffold from the word ⊢∈⊤⊥≺⊙⊞⊡∋⊣ — the falsifiability
-- argument as an IMASM word, verified by the kernel's own instruments:
--   banked: OK — weight survived 1 live clear(s) by being banked
--   cycle:  period 10, verdict T at every cut (kernel trace, hosted run).
-- The word's frame-before-deposits order (δ before δ, μ after μ) is the
-- claim that a result held in the frame survives the reversal; the
-- falsifier's deposits outside the frame are cleared and inert.

import Imscribing.IGMorphism
import Imscribing.IGFunctor

namespace Imscribing
open Primitives Frobenius IGProtocol
open Dimensionality Topology Relational Polarity Grammar
     Fidelity KineticChar Granularity Criticality Protection Stoichiometry Chirality

-- ── Token → IG field mapping ──────────────────────────────────────────────
--   [0] VINIT     dim    := 𐑼               𐑼 → 𐑚  | initial object — ground of distinction
--   [1] FSPLIT    gran   := 𐑚               𐑚 → 𐑚  | split δ — range decomposition
--   [2] EVALT     crit   := ⊙               𐑚 → 𐑙  | evaluate-true — criticality gate open
--   [3] EVALF     chir   := 𐑖               𐑚 → 𐑙  | evaluate-false — chirality check
--   [4] AREV      pol    := 𐑗               𐑚 → 𐑙  | reverse morphism — parity flip
--   [5] IMSCRIB   gram   := 𐑠               𐑚 → 𐑙  | identity — self-imscription
--   [6] ENGAGR    stoi   := 𐑳               𐑚 → 𐑙  | engage paradox — B-state, both arms
--   [7] IFIX      prot   := 𐑭               𐑚 → 𐑙  | irreversible fixation — winding number
--   [8] FFUSE     stoi   := 𐑙               𐑙 → 𐑡  | fuse μ — assembly mode
--   [9] TANCH     top    := 𐑡               𐑙 → 𐑼  | terminal object — connectivity boundary

-- ── Stage Imscriptions (per-node cumulative) ────────────────
private def falsifiability_degenerate_s0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def falsifiability_degenerate_s1 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def falsifiability_degenerate_s2 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def falsifiability_degenerate_s3 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def falsifiability_degenerate_s4 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := monad, chir := sure, stoi := hung, prot := awe }
private def falsifiability_degenerate_s5 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := awe }
private def falsifiability_degenerate_s6 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := awe }
private def falsifiability_degenerate_s7 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := up, prot := ah }
private def falsifiability_degenerate_s8 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }
private def falsifiability_degenerate_s9 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := measure, crit := monad, chir := sure, stoi := hung, prot := ah }

-- ── Label Imscriptions (per-node delta) ─────────────────────
private def falsifiability_degenerate_l0 : Imscription :=
  { dim := array, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def falsifiability_degenerate_l1 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := thigh, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def falsifiability_degenerate_l2 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := monad, chir := fee, stoi := hung, prot := awe }
private def falsifiability_degenerate_l3 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := sure, stoi := hung, prot := awe }
private def falsifiability_degenerate_l4 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def falsifiability_degenerate_l5 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := measure, crit := woe, chir := fee, stoi := hung, prot := awe }
private def falsifiability_degenerate_l6 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := up, prot := awe }
private def falsifiability_degenerate_l7 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := ah }
private def falsifiability_degenerate_l8 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }
private def falsifiability_degenerate_l9 : Imscription :=
  { dim := dead, top := judge, rel := ado, pol := church, fid := age, kin := yea, gran := bib, gram := vow, crit := woe, chir := fee, stoi := hung, prot := awe }

-- ── Main IGProtocol term ────────────────────────────────────
noncomputable def falsifiability_degenerate_protocol : IGProtocol falsifiability_degenerate_s0 falsifiability_degenerate_s9 :=
  .withGram Grammar.measure <|
  -- Dual-Link self-pairing: .prod arms fuse via tensorProduct falsifiability_degenerate_s8 falsifiability_degenerate_s8 = falsifiability_degenerate_s8 (idempotent)
  (.seq (.arrow falsifiability_degenerate_l0 falsifiability_degenerate_s0 falsifiability_degenerate_s1) (.seq (.prod (.arrow falsifiability_degenerate_l1 falsifiability_degenerate_s1 falsifiability_degenerate_s8) (.arrow falsifiability_degenerate_l1 falsifiability_degenerate_s1 falsifiability_degenerate_s8)) (.seq (.arrow falsifiability_degenerate_l8 falsifiability_degenerate_s8 falsifiability_degenerate_s8) (.arrow falsifiability_degenerate_l8 falsifiability_degenerate_s8 falsifiability_degenerate_s9))))

-- ── Evaluation arm sub-defs ───────────────────────────────────

-- deposit T
noncomputable def falsifiability_degenerate_true_arm : IGProtocol falsifiability_degenerate_s0 falsifiability_degenerate_s9 :=
  (falsifiability_degenerate_protocol).restrictToEVALT

-- deposit F (the falsifier's landing)
noncomputable def falsifiability_degenerate_false_arm : IGProtocol falsifiability_degenerate_s0 falsifiability_degenerate_s9 :=
  (falsifiability_degenerate_protocol).restrictToEVALF

-- ── Verification theorems ─────────────────────────────────────

-- Tier: apply the Grammar to the object (self-application). assess_tier verdict on the imscribed tuple: .O₂dag.
def falsifiability_degenerate_tier_ground : OuroboricityTier := TierFunctor.obj falsifiability_degenerate_s0
def falsifiability_degenerate_tier : OuroboricityTier := TierFunctor.obj falsifiability_degenerate_s9
#eval falsifiability_degenerate_tier_ground  -- tier of the ground (pre-transformation)
#eval falsifiability_degenerate_tier  -- the Grammar's own verdict on the closed object

-- Frobenius (split → fuse): μ∘δ = id on the ground imscription
theorem falsifiability_degenerate_frobenius :
    igFrobeniusAlg.mul falsifiability_degenerate_s0 falsifiability_degenerate_s0 = falsifiability_degenerate_s0 :=
  igFrobAlg_self_fusion falsifiability_degenerate_s0
